# PATTERN.md — チャートパターン検出（仕様）

19 条件のスクリーナー（`docs/SCREENER_CLOSING.md`）に代わる新しい判定。
7 パターンをチャート形状として検出する。

**この文書が正の情報源である。** ここに書いていない判断が必要になったら、実装せず
§4 の質問ログに追記して止まる（CLAUDE.md）。

## 0. 前の設計から引き継がないもの

`docs/SCREENER.md` の 19 条件・閾値・D-1/D-2 の判定規則・Q-2 は**引き継がない。**
あちらは記録の列が何かを引くために読むだけで、条件は持ち込まない（CLAUDE.md）。

引き継ぐのは実装だけである。

| 引き継ぐ | どこ |
| --- | --- |
| スイング検出（fractal 方式・確定ラグ k 本） | `features/swings.py` |
| ATR・SMA などの指標 | `features/indicators.py` |
| ユニバース・保存・日次スナップショット | `universe/`・`data/` |
| 配信記録と結果付け | `screener/record.py`・`resolver.py` |
| LINE 配信経路・画像カードの描画 | `notify/`・`render/` |
| 33 業種の等加重リターン | `features/sector.py` |

## 1. 事前登録したパラメータ（2026-09-08 決定・検出数を見る前）

**この 4 つは検出数を見てから動かさない。** 動かし始めると、検出数を見ながら形を作る
ことになる（設計責任者の明示指示）。数値の根拠が薄いことは承知のうえで固定する。

| 記号 | 値 | 何に使うか |
| --- | --- | --- |
| **ε（水平とみなす傾き）** | **日次 0.1% 未満** | 水平な上辺・下辺・平行の判定 |
| **旗竿** | **5 営業日以内に 10% 以上の上昇** | フラッグ／ペナントの直前の急騰 |
| **タッチ点数** | **3 + 2（最小 5）** | 回帰で 2 本の線を引くときの各辺の点数 |
| **等値とみなす幅** | **±1.5%**（LMW） | 2 つ以上の極値が「同じ高さ」かの判定 |
| **探索ウィンドウ** | **60 営業日** | 全極値がこの範囲に収まること |
| **ダブルボトムの最小間隔** | **22 営業日**（LMW） | 2 つの安値の距離。上限はウィンドウが兼ねる |
| **トリプル／逆三尊の隣接間隔** | **10 営業日** | 隣り合う谷どうしの距離。下限のみ |
| **ボックスの水平許容** | **±0.75%**（LMW） | 上辺・下辺それぞれの点が平均から離れてよい幅 |

**±0.75% は ±1.5% とは別の定数である。** ボックスの辺は「複数点が 1 本の水平線に
乗っている」ことを要求するので、2 点が等値かの判定より狭い。片方を他方から導かない。

**探索ウィンドウを 60 営業日にした理由**: LMW の 35 日はカーネル平滑化後の系列を
前提にした値で、生のスイング列には短すぎる（設計責任者の判断）。

**±1.5% は ATR 連動にしない。** 銘柄のボラティリティで許容幅が変わると、同じ「ダブル
ボトム」が銘柄ごとに別の形を指すことになる。固定率で揃える（設計責任者の判断）。

これらを変えるときは §4 の質問ログを通し、**変更前に**決める。検出数を見たあとの変更は
「形を数字に合わせた」ことになるので、記録に残したうえで別物として扱う。

## 2. 対象パターン

**7 パターン。上昇ウェッジは対象外**（2026-09-08 決定）。

### 2.1 反転系（先に実装する）

極値は `swings.py` の**確定済み交互スイング**（`swings_as_of`）だけを使う。
安値と高値が交互に並ぶ列の、末尾側から必要な本数を取る。

#### 共通

| 項目 | 決め方 |
| --- | --- |
| ネックライン | **水平線**。2 点を結ぶ斜めの線は使わない（傾きを許すと許容傾きという新規パラメータが要る。ε を流用すると意味が変わる） |
| 完成 | **終値がネックラインを上抜けた日 T に成立**。極値が揃っただけでは成立させない |
| 確定ラグ | `swings.py` の k=3 だけ。**LMW の確定ラグ 3 本は使わない**（二重にかけると 6 本遅れる） |
| ウィンドウ | 使う極値がすべて T から遡って **60 営業日以内**に収まること |

#### R1 ダブルボトム

| | |
| --- | --- |
| 極値 | 安値 L1 → 高値 H → 安値 L2 |
| 等値 | `\|L1 − L2\| / ((L1 + L2) / 2) ≤ 0.015` |
| 間隔 | L1 と L2 が **22 営業日以上**離れる |
| ネックライン | 谷間の高値 H（1 点・水平） |
| 完成 | `終値[T] > H` |

#### R2 トリプルボトム

| | |
| --- | --- |
| 極値 | 安値 L1 → 高値 H1 → 安値 L2 → 高値 H2 → 安値 L3 |
| 等値 | 3 谷の平均 `m = (L1+L2+L3)/3` に対し、**各谷が m の ±1.5% 以内** |
| 間隔 | 隣接する谷（L1-L2・L2-L3）が**それぞれ 10 営業日以上**離れる |
| ネックライン | `max(H1, H2)`（谷間の高値 2 点のうち高いほう・水平） |
| 完成 | `終値[T] > max(H1, H2)` |

#### R3 逆三尊（逆ヘッドアンドショルダー）

| | |
| --- | --- |
| 極値 | 安値 E1（左肩）→ 高値 E2 → 安値 E3（頭）→ 高値 E4 → 安値 E5（右肩） |
| 肩 | E1 と E5 が両者の平均の **±1.5% 以内** |
| ネックライン点 | E2 と E4 が両者の平均の **±1.5% 以内** |
| 頭 | `E3 < E1` かつ `E3 < E5`。**深さの下限は設けない** |
| 間隔 | 隣接する谷（E1-E3・E3-E5）が**それぞれ 10 営業日以上**離れる |
| ネックライン | `max(E2, E4)`（水平） |
| 完成 | `終値[T] > max(E2, E4)` |

**R2 と R3 は別パターン**（D-2）。R2 は 3 谷が等値、R3 は中央が両側より低い。
E3 が 3 谷の平均 ±1.5% に収まりつつ E1・E5 の両方より低いことはありうるので、
**同じ 5 極値が両方に該当する形が出うる。その場合は両方を記録する**（どちらかに寄せない。
どちらの形として出やすいかは、検出数を見てから設計責任者が判断する）。

### 2.2 保ち合い系（反転系の確認後・未実装）

| # | パターン | 判定式 |
| --- | --- | --- |
| C1 | 上昇三角 | `\|上辺傾き\| < ε`、`下辺傾き > 0`、各線に 3+2 タッチ、`終値 > 上辺` |
| C2 | 上昇ボックス | 上辺の各点が上辺平均 **±0.75%** 以内、下辺も同様、`最低の山 > 最高の谷`、`終値 > 上辺` |
| C3 | 上昇フラッグ | 旗竿あり、上辺・下辺とも `傾き < 0` で平行（**傾き差が ε 未満**）、**15 営業日未満**、`終値 > 上辺` |
| C4 | 上昇ペナント | 旗竿あり、`上辺傾き < 0`・`下辺傾き > 0`（収束）、**15 営業日以下**、`終値 > 上辺` |

フラッグは 15 営業日**未満**、ペナントは 15 営業日**以下**（指示のまま。境界の扱いが
違うので実装時に取り違えないこと）。

## 3. 記録

配信記録・結果付けの仕組みは `SCREENER.md` §3 のものをそのまま使う。
パターン名と、判定に使った極値の位置・値を列に足す予定。**列の定義は反転系の検出数を
確認してから書く**（配信を再開するときに合わせて決める）。

## 4. 質問ログ

### Q-1（2026-09-08 提起・2026-09-08 クローズ）7 パターンの判定式

**判定式が手元に無い。** 設計責任者は「今回のレポートで判定式は揃った」としているが、
そのレポートはこのリポジトリにも会話にも入っていない。

リポジトリ内で確認できるのは `docs/RESEARCH.md` §2.1 の 1 行だけで、そこにあるのは
LMW の**方法論**（カーネル平滑化、35 日＋確定用 3 日、条件付き分布 vs 無条件分布）であり、
**パターンごとの判定式でも ±1.5% の出典でもない。**

推測で埋めない（CLAUDE.md）。足りないものを具体的に挙げる。

**反転系 3 つ（先に要るもの）:**

1. **±1.5% を何に当てるか。** ダブルボトムなら 2 つの安値どうし、で読める。
   トリプルボトムは「3 つが互いに ±1.5%」か「1 つ目を基準に各々 ±1.5%」か。
   逆三尊は「両肩どうしが ±1.5%」か、頭の深さにも条件があるか
2. **ネックラインの引き方。** 間の戻り高値 2 点を結ぶか、水平（ε 判定）を要求するか
3. **完成の定義。** ネックライン抜けを待つか、極値が揃った時点で検出とするか。
   LMW はウィンドウ内で完成したものだけを扱い確定ラグ 3 本を置くが、その 3 本を
   ここでも使うか（`swings.py` の確定ラグ k とは別のもの）
4. **探索ウィンドウ。** LMW の 35 日を採るか、別の長さか
5. **極値の間隔。** 2 つの安値が近すぎる／離れすぎている場合の下限・上限

**保ち合い系 4 つ:**

6. **どの 4 つか。** 上昇ウェッジを除いた 7 のうち、反転系 3 つ以外の 4 つの名前

**回答を受けて §2 に展開し、クローズした。** 新しく決まった 4 つの数値（探索ウィンドウ
60 営業日、ダブルボトムの最小間隔 22 営業日、トリプル／逆三尊の隣接間隔 10 営業日、
ボックスの水平許容 ±0.75%）も §1 に事前登録した。**これらも検出数を見てから動かさない。**

判定式の外側で決まったこと:

- **斜めのネックラインは使わない。** 許容傾きという新規パラメータが要るため。
  ε を流用すると意味が変わる
- **LMW の確定ラグ 3 本は使わない。** `swings.py` の k=3 と二重になり 6 本遅れる
- **探索ウィンドウは LMW の 35 日ではなく 60 日。** 35 日はカーネル平滑化後の前提

## 5. 決定ログ

### D-1（2026-09-08）上昇ウェッジを対象外にする

7 パターンで進める。上昇ウェッジは含めない。

### D-2（2026-09-08）トリプルボトムと逆三尊は別パターン

中央の極値が両側より低いか等値かで区別する。1 つにまとめない。

### D-3（2026-09-08）§1 の 4 パラメータを事前登録する

ε＝日次 0.1%、旗竿＝5 営業日以内に 10% 上昇、タッチ点数＝3+2（最小 5）、
等値幅＝±1.5%（ATR 連動にしない）。**検出数を見てから動かさない。**

### D-5（2026-09-08）判定式と追加パラメータ

Q-1 の回答を §2 に展開した。追加で事前登録した数値は 4 つ（探索ウィンドウ 60 営業日、
ダブルボトムの最小間隔 22 営業日、トリプル／逆三尊の隣接間隔 10 営業日、ボックスの
水平許容 ±0.75%）。**検出数を見てから動かさない。**

ネックラインは水平のみ。完成は終値の上抜け。確定ラグは `swings.py` の k のみ。

### D-4（2026-09-08）実装は反転系 3 つから

保ち合い系 4 つは、反転系の検出数と形を目視確認してから。
