# TASKS.md — 作業分割と受け入れ条件

**2026-08-30: プロジェクト終了。** DESIGN.md §10.3 第1関門（内部確認窓）不通過。
詳細は `CLOSING.md`。M4-L2・M5 は実施せず（該当セクションに記載）。

対応文書: SPEC.md（決定）、DESIGN.md（設計）、CLAUDE.md（作業規約）、CLOSING.md（終了記録）
順番: M1 更新 → M2 → M3a → M4-L1 → **判定（Fable）** → M3b → M4-L2 → M5
L1 の判定が出るまで M3b（表示・テンプレート校正）と M5（描画・配信）には着手しない。

各タスクの完了条件は「受け入れ条件をすべて満たし、必須テストが通り、`python -m unittest discover -s tests -t .` が全件 OK」。
判断が必要な箇所（DESIGN.md に書いていない閾値・方式）は実装せず、質問として TASKS.md 末尾の「質問ログ」に追記して止まる。

---

## M1 データ基盤（実装済み。設計に合わせて更新）

### T-101 設定と索引の追加
- 目的: DESIGN.md §12 のパラメータを `config.py` に追加（k, pullback_max_days, pullback_max_depth, shallow_depth, deep_depth, pool_days, sector_cap, corr_threshold, top_n, label_n）
- 受け入れ: 環境変数で上書きでき、既定値が §12 と一致。`Settings.from_env()` のテスト追加

### T-102 指数データ
- 目的: TOPIX（`^TPX`、失敗時 `^N225`）の日足を取得し store に `ticker="__IDX__"` として保存。DRYRUN は合成
- 受け入れ: `fetch_index()` が 300 本以上返す。D2 と地合いゲージが参照できる
- テスト: 合成指数で RS 計算が再現一致テストを通る

### T-103 バックフィル（検証用 10 年）
- 目的: `python -m stockbot.cli backfill` で `HISTORY_DAYS=2600` 相当を取得し store に保存。通常の daily とは別コマンド。締切ガードと中断再開（store に既にある銘柄はスキップ）
- 受け入れ: 中断後に再実行すると残りだけ取得する。取得率が fetch_meta に記録される

### T-104 参照データの取得経路（調査込み）
- 目的: JPX の決算発表予定日・上場廃止銘柄一覧を自動取得する。まず JPX 公開ページの URL と書式を調べ、取れるなら `jpx_lists.py` に追加、取れなければ現行の CSV 手動運用のままとし、調査結果を `docs/DATA_SOURCES.md` に書く
- 受け入れ: 取得できた場合、`load_earnings_schedule` / `load_delistings` が同じ列で読める。規約上の制約を明記
- 注意: 取得の可否が分からない場合は実装を止めて質問ログへ

---

## M2 特徴量

### T-201 指標
- 目的: `features/indicators.py`: SMA(5/25/75/200)、Wilder ATR14、TR、単純 ATR5/ATR20、BB 幅、週足集約（W-FRI、確定週のみ）
- 受け入れ: 既知の小さな系列で手計算と一致。週足集約で T を含む週が除外される
- 必須テスト: 再計算一致（DESIGN.md §11）を各指標に適用

### T-202 スイング検出
- 目的: `features/swings.py`: DESIGN.md §3 の fractal 検出、確定ラグ、交互化。戻り値はインデックス・種別・値・確定インデックスの表
- 受け入れ: 合成系列で期待どおりのスイングが出る。交互化で同種連続が残らない
- 必須テスト: (1) T で切った検出 = 全期間検出を i ≤ T−k で絞ったもの、(2) i > T−k のスイングが含まれない

### T-203 押し目構造と状態
- 目的: `features/pullback.py`: DESIGN.md §4 の H0/L0/Lp/R と基本量、トリガー、状態（完了/失効/ブレイク/反発開始/形成中）、浅押し・深押しフラグ
- 受け入れ: 合成「pullback」系列で 形成中 → 反発開始 → 完了 の順に状態が遷移する。失効条件が各々単独で効く
- 必須テスト: 状態が毎日ちょうど一つ。再計算一致。Lp ≤ L0.low で失効

### T-204 特徴量 D1〜D8
- 目的: `features/dimensions.py`: DESIGN.md §5 の全特徴量を、ID・値・方向・暫定帯・次元のメタデータ付きで返す。欠損は NaN
- 受け入れ: 全 ID が DESIGN.md の表と一致（テストで ID 集合を比較）。形成中では D6 が欠損
- 必須テスト: 再計算一致（全特徴量・複数 T）。欠損数のカウント

### T-205 再計算一致テストハーネス
- 目的: `tests/test_lookahead.py`: 任意の特徴量関数に対して「T で切る／全期間から取り出す」を比較する共通ハーネス。合成 10 銘柄 × T 5 点
- 受け入れ: 新しい特徴量を追加したら自動的に対象に入る（メタデータから列挙）

### T-206 日次特徴量の保存
- 目的: `daily/features_YYYY-MM-DD.csv.gz` に全採点銘柄の特徴量・状態・次元スコア・総合スコア・地合いを保存（前向き検証用）
- 受け入れ: 列名が安定し、後日 resolver（T-504）が読める

### T-207 地合いゲージとブレス
- 目的: DESIGN.md §8.1 の 6 点ゲージ。指数と store 全体から計算
- 受け入れ: 合成データで 強/中/弱 が出る。再計算一致

### T-208 ゲート G0〜G3
- 目的: `features/gates.py`: DESIGN.md §2 の 4 ゲートを判定する
  - G0: ユニバース通過（universe/latest.csv の passes）、かつ T から label_n 営業日以内に
    決算発表が無い（earnings_schedule が空、または当該銘柄の予定日が取れない場合は
    この条件を省略して True とし、`g0_earnings_unknown = True` を立てる）
  - G1: SMA75[T] > SMA200[T] かつ SMA200[T] > SMA200[T-30]
  - G2: Close[T] >= SMA200[T]
  - G3: Close[T] >= 0.75 * max(High[T-251..T])
- 戻り値: 各ゲートの bool と総合 `gate_pass`、および g0_earnings_unknown
- pipeline.py: 採点対象を「gate_pass かつ 状態が形成中/反発開始/ブレイク」に変更する。
  日次特徴量ファイルには gate 列も保存し、ゲート落ちの内訳がログに出るようにする
  （G1 落ち / G2 落ち / G3 落ち / 決算接近 の件数）
- 受け入れ: 合成データで各ゲートが単独で効く。決算データが無い場合も落ちない
- 必須テスト: 再計算一致（DESIGN.md §11）。SMA200[T-30] の履歴が足りない日は False

---

## M3a スコア合成（L1 に必要な部分）

### T-301 プール正規化と合成
- 目的: `scoring/composite.py`: DESIGN.md §6。20 日プール百分位、∩ の帯変換、二値、欠損 0.5、次元平均、状態別次元集合、V1/V2/V3
- 受け入れ: 同じ入力で V1/V2/V3 が再現。プールの日数が足りない初期は利用可能な日数で計算しその旨をログ
- 必須テスト: 帯変換の境界値（帯内 1、帯幅分外で 0）。次元欠損時の扱い

### T-302 テンプレート適合度（初期 W）
- 目的: `scoring/template.py`: DESIGN.md §7 の格子化、W 初期値、価格・出来高の 2 テンプレート
- 受け入れ: 合成の理想経路 > 崩れた経路（L0 割れ、押し目前半で H0 超え）
- 必須テスト: 再計算一致。列ごとの P の合計が 1

---

## M4-L1 検証レイヤー 1

### T-401 ラベル
- 目的: `validation/labels.py`: DESIGN.md §9。超過リターン h ∈ {3,5,10,15,20,30}、成功/失敗/未決（N=10/15/20）、MFE/MAE
- 受け入れ: 手計算の小例と一致。同日に H0 超えと Lp 割れがある日は失敗
- 必須テスト: T 以前の行を変えてもラベルが変わらない

### T-402 歴史的再生（リプレイ）
- 目的: `validation/replay.py`: 主評価窓・頑健性窓の各営業日 T について、T で切った系列から M2/M3a を走らせ、銘柄×日の特徴量・スコア・状態・ラベルを 1 つの表に蓄積（parquet/csv.gz）。ホールドアウトは生成しない（フラグで明示的に有効化）
- 受け入れ: 1 年分の再生が所要時間とともにログされ、中断再開できる。ユニバースは T 時点で履歴 250 本以上の銘柄
- 必須テスト: 再生の 1 日分 = 日次パイプラインを同じ T で走らせた結果（同一性）
- **R1.1 で追加**: 主評価窓（2021-08-01〜2026-01-30）の replay は生成済み（1,175 ファイル・362,461 行、
  D2 欠損率 0%）。R1 は新規特徴量を追加しないため再生成は不要。**頑健性窓
  （DESIGN.md §10.1 開始日規則〜2021-07-31）の replay 生成が残タスク。**
  開始日は `scripts/compute_robustness_start.py`（`probe-robustness-start.yml`、
  2026-08-26 実行、store 最古日 2016-02-03 に基づく実測）により算出済み:
  - store 最古日から 272 営業日後: 2017-03-14 → その翌営業日: **2017-03-15**
  - 事前登録の下限 2017-03-01 より遅いため、規則側（2017-03-15）を採用
  - 開始日時点のユニバース通過銘柄数: **2,815 銘柄**。これは「現在の上場銘柄のうち
    その日時点で履歴250本以上ある銘柄数」（`replay.replay_universe_tickers` の定義。
    DESIGN.md §10.1 がreplayのユニバースをそう定義している）であり、本番の
    `universe.build`（20日平均売買代金≥2億円・株価≥200円・分割疑いなし等を含む）
    とは別物（本番ユニバースの通過数であれば明らかに多すぎる数値になる）
  - よって頑健性窓の**集計対象期間**は **2017-03-15 〜 2021-07-31**（stats_start）
  - backfill の延長・`MIN_HISTORY_BARS`・`MAX_EMPTY_DAY_RATE` はいずれも変更しない。
    この期間で再度ガードが発火した場合は回避策を取らず、質問ログに追記して止まる
  - この開始日・通過銘柄数は T-405 のレポート冒頭にも記録すること
  - **2026-08-26 実行条件の確認（`probe-robustness-start.yml` 再実行）で追加測定**:
    - **G3 計算可能日（252本目）: 2017-02-13。** replay の実際の `--start` はこの日
      にする（§6.3 プールの20日ウォームアップも兼ねる）。2017-02-13〜2017-03-14
      （stats_start 未満）は `--stats-start 2017-03-15` により warmup=True で出力し、
      T-403 の集計からは除外する（プール継続のためだけに計算する）
    - **指数(`__IDX__`)カバレッジ（初回測定。g3_start=2017-02-13基準、warmup区間を含む
      replay実行対象の営業日で計測。集計対象=stats_start=2017-03-15基準ではない点に
      注意——この2つの基準を区別せず「1,111日」とだけ書いた初回記録を訂正）**:
      2017-02-13〜2021-07-31 の1,111営業日中1,090日に指数データあり（欠落21日・1.89%）。
      欠落は2018-01-01〜01-03（年始、3営業日連続）が最大で、他は単発1日が17件散発
    - **主評価窓との対称性を測定（完了）**: `scripts/compute_robustness_start.py`を
      拡張し、主評価窓・頑健性窓（g3_start基準・stats_start基準の両方）の欠落率を
      並べて報告するようにした（`probe-robustness-start.yml` run #3、2026-08-27実行）。
      **実測: 主評価窓 0.00%（欠落0日）vs 頑健性窓(集計対象) 1.93%（欠落21日）。**
      機械的なパーセンテージ差のしきい値（|diff|<2%）で「同程度」と自動判定する
      ロジックを最初に組んだが、0%と非0%を同列に扱う欠陥があり誤判定だったため、
      自動判定は撤去し数値の記録のみに変更した（このスクリプト自体の反省点。
      検証結果の解釈はCLAUDE.mdにより設計責任者が行う）。0件と21件は明らかに
      非対称と判断し、前方補完を実施する側を採用した
    - **前方補完を実装（完了）**: `pipeline.py`の`compute_daily_features`で、
      `idx_close.reindex(close.index)`の後に`.ffill(limit=3)`を追加（過去方向のみ・
      最大3営業日。ffillは過去の値のみを使うため未来参照にならない）。主評価窓は
      欠落0件のためこの変更は実質無効（値は変わらない）。頑健性窓の21件の欠落は
      すべて連続3営業日以内（最大が2018年始の3営業日連続）のため、この変更で
      全件補完される見込み。テスト追加: `tests/test_pipeline.py`
      `IndexForwardFillTest`（3営業日以内は補完される・3営業日超は補完されない・
      補完値が未来の値でないことを確認）。`python -m unittest discover`: 287 tests OK
    - **既存replayへの影響**: チャンク1（2017-02-13〜2017-06-30）・主評価窓
      （362,461行）はいずれも対象期間内に指数欠落が無く、この修正による値の変化は
      無い（再生成不要）。頑健性窓のチャンク2以降（2017-07以降、既知の欠落日を含む）
      からこの修正が効いた状態で生成される
    - 1306.Tの配当落ちによる単日下落（対数リターン<-2%）は主評価窓50日・頑健性窓
      49日と近い頻度で、窓間の非対称は無いと判断（診断のみ・修正はしない。
      docs/DATA_SOURCES.md §4に記録済み）
    - **F別欠損率テーブル（実行条件1）はチャンク1だけでは完成しない**: 2017-02〜06は
      頑健性窓の左端で左側打ち切りが最も強い区間であり、スモークテスト（ガード発火の
      有無・処理速度の確認）としては正しい順番だが、条件1の判定（窓別のF別欠損率
      テーブル）は頑健性窓の全期間のreplayが揃ってから行う
    - **replay の実行は主評価窓と別アーティファクト**（`replay-robustness.yml`、
      `data/replay_robustness`、cache key `replay-robustness-v1-`）で行う。
      `data/replay`（主評価窓・362,461行）には一切書き込まない
    - 実行手順: `python -m stockbot.validation.replay --start 2017-02-13 --end 2021-07-31
      --output-dir data/replay_robustness --stats-start 2017-03-15`
      （`replay-robustness.yml` の workflow_dispatch 経由。半年〜1年単位で区切る）
  - **主評価窓 no-op 検証（PR #36後）: 完了。再生成はしない・9900.Tを両窓から
    除外する（2026-08-27、実行条件の指示への対応。理由を2回訂正済み）**
    - `verify_d2_recompute.py`の実測: 362,461行中12行（d2_rs60/rs120/rsline_posの
      3列とも同じ12行）が不一致。`verify_full_recompute.py`（D1〜D8・構造系・
      ゲートに拡大）の実測: 9900.T全170行中40行に拡大（新たに2025-08-25〜09-18の
      期間も判明）。**他3459銘柄はサンプル検査（各1行）で不一致ゼロ**
    - **原因**: サガミホールディングス(9900.T)の株式分割（基準日2026-08-30、
      効力発生日2026-08-31、1→2株）を、yfinanceが効力発生前の2026-08-27時点で
      過去の終値・出来高に先出しで遡及適用していた（`revisions.csv.gz`:
      `observed_on=2026-08-27, old_close=1769.0, close=884.5`で比率2.0、
      `old_volume=56300, volume=112600`も比率2.0）
    - **境界の実在: 実データで確認済み（確定）**。先出し適用は「全履歴に対する
      一律スケール」ではなく、**2025-01-08を境界とする部分適用**だった。
      `inspect_ticker_revision.py --ticker 9900.T --around-date 2025-01-08
      --window-days 45`（`probe-robustness-start.yml` run #10）の実測で、
      storeのClose系列に実際の段差を確認:
      ```
      2025-01-06  1755.0
      2025-01-07  1752.0
      2025-01-08   865.0   <- 段差（比率≈2.02）
      2025-01-09   859.0
      ```
      `revisions.csv.gz`の9900.T最古の改訂記録も同じ2025-01-08付け
      （`old_close=1730.0, close=865.0`、比率ちょうど2.0）で一致。構造系特徴量に
      不一致が出ていた事実（一律スケールなら比率系のD2だけが変化し構造系は
      不変のはず）が部分適用の傍証だったが、それが実データで裏付けられた。
      不一致が2025-02-20〜03-21（60/120日参照）と2025-08-25〜09-18（200日SMA・
      G3の252日参照）の2帯に分かれていたのも、いずれかのルックバック窓が
      この境界をまたぐタイミングと符合する
    - **再生成しない理由（2回目の訂正。正しい論拠）**: 「再生成してもいずれ別の
      ドリフトを拾うだけだから」ではない（これはどんな規模の不一致でも再生成を
      不要にしてしまう論法で、一般には使えない）。**正しい論拠は、再生成すると
      データが悪化するから。** 既存replayの9900.Tは（分割前の）正しい価格で
      計算されているが、いま再生成すると現在のstoreにある誤った先出し半額価格が
      焼き付く。再生成は正しさを減らす方向にしか働かない
    - **結論: 9900.Tを主評価窓・頑健性窓の両方の集計から除外する（理由:
      データ品質）。** 除外対象は170行/362,461行=0.05%で、データの損失は
      ごく僅か。データ品質による除外のため検定数は増えない。除外はT-403の
      集計（`layer1.py`、未実装）が実装され次第、除外リストとして反映する
      （具体的な除外行の特定は`verify_full_recompute.py`の実測結果を参照。
      不一致が出た日だけでなく、9900.T自体を丸ごと除外する）
  - **再生成の要否を判定する閾値（事前登録は2026-08-27時点、結果を見た後に
    訂正。訂正後の基準を以後の判断に使う）**:
    当初「(a) 不一致行が全体の0.1%超、または(b) 構造系特徴量に不一致」で
    再生成としていたが、これは「原因不明の不一致を見逃さない」ための基準であり、
    原因が既に特定できていて、かつ再生成がデータを悪化させる場合には適用しない
    ことが判明した。**訂正後の基準**:
    - 原因不明の不一致が1件でもあれば再生成する（0.1%等の閾値を設けない。
      「不一致の存在」自体が判断材料であり、量の多寡で見逃さない）
    - 原因が特定でき、かつ再生成がデータの正しさを減らす場合は、再生成でも
      放置でもなく、**該当銘柄をデータ品質により集計から除外する**
    - 原因が特定でき、かつ再生成がデータの正しさを増す（＝現在のstoreの方が
      正しい）場合は再生成する
  - **no-op検証をD2の3列から全列に拡大: 完了**。`verify_full_recompute.py`を
    追加し、9900.T全行＋他3459銘柄を1行ずつサンプリングして実行（3629行・163秒）。
    結果は上記のとおり9900.T以外に不一致なし
  - **分割予定公表・効力発生前の銘柄の全store点検（実施済み・なお限定的）**:
    `scan_pending_split_adjustments.py --recent-days 36500`で`revisions.csv.gz`
    の全履歴797件を走査（直近3日だけでなく全期間に拡大済み）したが、9900.T
    以外に「先出し適用」の疑いがある銘柄はゼロだった。toushi-radar.com調べの
    JPX分割公表済み・効力発生前リストから9900.T以外の直近効力発生4銘柄
    （6834精工技研・7649スギHD・8011三陽商会・9279ギフトHD、いずれも
    2026-09-01効力発生）も同スキャンに含めて確認したが該当なし。
    **ただし走査対象が「revisions.csv.gzに記録された改訂」に限られるため、
    初回取得時点から既に誤った価格だった銘柄（改訂として記録されないケース）
    は依然として捕捉できない。** この死角は残存。指示にある「数十銘柄」規模の
    突合は、toushi-radar.com（直近3か月分）の1ソースのみに基づく限定的な確認
    にとどまっており、より網羅的なJPX公式リストとの突合は未実施
  - **`adjust.py`の`suspected_unrecorded_split`が9900.Tで発火しなかった理由:
    特定済み**。`cli.step_fetch`/`step_backfill`はいずれも`check_all(ohlcv)`を
    **その回に新規取得したデータだけ**に対して、`store.upsert`でstoreへ
    マージする**前**に実行する（`cli.py`）。日次fetchの取得窓は`HISTORY_DAYS=400`
    （既定）で、今日（2026-08-27）から遡ると約2025-07-24までしか含まない。
    先出し適用の境界が2025-01-08前後だとすると、日次fetchの取得窓に含まれず、
    `check_splits`の連続バー比較（`close[i-1]/close[i]`）が境界をまたいで
    比較する機会が一度も無かったことになる。backfill（`HISTORY_DAYS=2600`）
    なら境界を含み得るが、backfillは「store本数が閾値以上の銘柄をスキップ」
    する中断再開ロジックのため、既に十分な本数を持つ9900.Tは再取得対象から
    外れ、この長期チェックの機会も得られなかった。**「全期間一律スケールは
    構造的に検出できない」という単純化は誤りで、正しくは「マージ前・単発fetch
    の取得窓のみを見るため、窓の外にある境界は検出しようがない」。** 部分適用
    （境界あり）の場合でも、その境界が現在の取得窓の外にあれば見逃す
  - **本番対応: 9900.Tを2026-08-31まで配信対象から除外（完了）**。
    `data/reference/manual_exclusions.csv`（`load_manual_exclusions`、
    `cli.step_universe`に配線済み）に追加。`until`日付を過ぎたら自動的に
    除外を終了する時限式（PR #44、マージ済み）
  - **3回目の訂正: 効力発生日と権利落ち日の取り違え（2026-08-28、設計責任者の
    指摘）**。基準日2026-08-30・効力発生日2026-08-31に対し、T+2で権利付最終日が
    8/26、**権利落ち日は8/27**。価格系列の分割調整は効力発生日ではなく
    権利落ち日に入るのが正常な挙動であり、`revisions.csv.gz`の
    `observed_on=2026-08-27`は「先出し」ではなかった。よって「古い値が正しく
    新しい値が誤り」という2回目の訂正も誤りだった
    - **再生成不要の理由も変わる**: 一律スケールは比率系の特徴量
      （depth_pct・retrace・position・dev5、depth_atrは分子分母とも同スケール、
      ADVもClose×Volumeで0.5×2.0=1.0）をすべて相殺するため、旧replay（未調整
      スケール）と新系列（調整済みスケール）は、双方とも内部無矛盾であれば
      同じ値を出すはず。40行の不一致は「旧replayが誤っていた」のではなく、
      「8/25〜8/27の過渡状態（history_days=400本の取得窓だけ書き換わり
      2025-01-08に段差が立ったstore）と比較したこと」自体が原因
    - **決定的検証: 完了。一致しなかった（予測は2回連続で外れた。以後は
      予測せず実測のみ報告する）**。data-daily実行（2026-08-28、head_sha
      `f1f71f7`、storeを最新化済み）後に`verify_full_recompute.py
      --focus-ticker 9900.T`を再実行した結果、170行中**41行が不一致**
      （前回40行から1行増加）。日付クラスタは変わらず2025-02-20〜03-21・
      2025-08-25〜09-18。「一律スケールなら一致するはず」という予測は不成立。
      単発フル履歴取得プローブ（yfinance直取得、storeを経由しない）で段差が
      消えていたことと、store（日次差分マージ）で段差が残っていることは
      矛盾しない: storeは`history_days`（既定400本）ぶんの日次窓でしか
      マージされないため、窓の外にある過去バーは未調整のまま残り得る
      （実データでの直接確認は次項）
    - **結論: 除外を維持（両窓・本番配信とも）**。次項の機械的規則により、
      9900.T単体の除外ではなく10銘柄の一括除外に置き換える
  - **ΔFの中間報告を停止（2026-08-28指示）**。第2関門は「全窓完成後の1回判定」
    と事前登録済みであり、チャンクごとの部分集計（ΔFのNW t）を見ながら
    続行・停止・調整を判断すると、その判断自体が検定として検定数を汚染する。
    チャンク4時点で頑健性窓のΔF（t=1.75）を実際に見てしまったため、
    **ここで閾値を固定し直し、以後動かさない**: ΔFのNW t ≥ 2、かつ効果量が
    設計窓の1/2以上（既存の第2関門の基準を再確認・再登録）。全窓完成まで、
    チャンクごとの報告はヘルス指標（F別欠損率・ガード発火状況・行数・空日）
    のみに留める。残り日数からのtの外挿もしない。`robustness_liquidity_diagnostics.py`
    （ΔF計算を含む）は`probe-robustness-start.yml`から呼ばなくなった
    （全窓完成後にT-403の最終集計として手動で実行する）
  - **押し目状態の行数が閾値未満の日を除外する規則を撤回（2026-08-28）**。
    2026-08-27に自ら事前登録した規則（`min_rows_per_day=30`）だったが、
    実測（チャンク4時点）で副作用が判明: 休場日の幻の行だけでなく、市場
    ストレス期（主評価窓2024-08-05〜15・2025-04-14〜16、頑健性窓
    2018-12-25〜2019-01-21）の正当な低候補日も一緒に落としていた。しかも
    両窓とも除外後にtが上がった（主評価窓2.34→2.49、頑健性窓1.75→1.85）。
    自分で入れた統計的除外ルールが結果を良くする方向に効くのは、まさに
    避けるべき形であるため撤回する。**主指標はDESIGN.md §10.2の全日版に戻す。**
    幻の行の根本原因（非取引日の混入）は統計的除外ではなくデータ側で対応した
    （次項）
  - **非取引日を日付軸から除去（完了、根本対応）**。`pd.bdate_range`はMon-Fri
    のカレンダー日を生成するだけで祝日を除外しないため、2018-07-16（海の日）
    のように休場日でも少数銘柄にデータの乱れがあると「幻の候補」が1〜2件だけ
    立つ日が生じる（チャンク4の空日調査で発見）。`validation/replay.py`に
    `_real_trading_days`を追加し、`run_replay`の日付軸生成に組み込んだ:
    その日にデータを持つ銘柄の割合が全体の`MIN_TRADING_DAY_COVERAGE`
    （既定0.5）未満の日は、非取引日として日付軸から除外する（外部の祝日
    カレンダーは持ち込まない、データ駆動の判定。実際の取引日は実測で約8割の
    銘柄にデータがあり、幻のデータは数銘柄に限られるため明確に分離できる）。
    テスト: `tests/test_replay.py` `RealTradingDaysTest`（実取引日は保持・
    幻日は除外・閾値境界の扱い・ログ出力を確認）。**既に生成済みのチャンク
    1〜6には未適用**（regenerateしていない。対象は実測で1日のみ確認済みだが、
    他にもあり得る。regenerateの要否は設計責任者の判断を待つ）
  - **check_splits を全銘柄・全期間で実行する体制を追加（完了。最重要の発見）**。
    `revisions.csv.gz`ベースの走査は`observed_on`が2026-08-25〜08-27の3日分・
    2銘柄しかカバーしておらず、「全履歴走査」として実質的に成立していなかった
    ことが`revisions_coverage.py`の実測で判明。改訂履歴ではなく系列内の段差
    そのもの（`adjust.check_splits`本来のロジック）を、storeの完全マージ済み
    系列（単発fetchの窓に限らない）に対して直接かける
    `scripts/check_splits_full_history.py`を追加し、
    `probe-robustness-start.yml`に組み込んだ
    - **実測結果（2026-08-28、3714銘柄検査）**: `suspected_unrecorded_split`
      が**10銘柄・12件**で検出された:

      | 銘柄 | 比率 | 該当日 |
      |---|---|---|
      | 1364.T | 10.0 | 2025-04-03 |
      | 3477.T | 0.5 | 2017-12-04 |
      | 4316.T | 2.0 | 2026-04-03 |
      | 5103.T | 3.0 | 2026-08-21 |
      | 6731.T | 0.5 | 2023-08-18〜12-04（3日） |
      | 6834.T | 5.0 | 2025-01-09 |
      | 7649.T | 2.0 | 2025-01-09 |
      | 7877.T | 0.2 | 2017-10-31 |
      | 7983.T | 0.2 | 2017-10-31 |
      | 9900.T | 2.0 | 2025-01-08 |

      6834.T・7649.Tは、以前JPX分割公表リストで確認した「9900.T以外の
      近日効力発生4銘柄」に含まれていた銘柄。`revisions.csv.gz`ベースの
      走査（観測3日分）ではゼロ件だったが、系列内の段差を直接見る方式で
      検出された。日付が9900.Tの2025-01-08と隣接（2025-01-09）しており、
      2026-08-25から`history_days`（400本）遡った位置に一致する
    - **窓別の内訳**: 頑健性窓（2017-03-15〜2021-07-31）= 3477.T・7877.T・
      7983.T／主評価窓（2021-08-01〜2026-01-30）= 1364.T・6731.T・6834.T・
      7649.T・9900.T／ホールドアウト（2026-02-01〜08-01）= 4316.T
    - **決定（2026-08-28、機械的規則。個別銘柄ごとの判断はしない）**:
      検出された10銘柄すべてを、主評価窓・頑健性窓・ホールドアウトの
      集計から一括除外する。3700銘柄中10銘柄=0.27%。9900.Tだけを除外して
      3477.T/7877.T/7983.Tを頑健性窓に残すと、このセッションで一貫して
      避けてきた窓間の非対称を自ら作ることになるため、機械的に全件を対象と
      する。データ品質による除外のため検定数は増えない。ホールドアウトは
      T-413未実装のため、この除外リストを事前登録しておく（結果を見る前。
      適用時に変更しない）。実装: `validation/layer1.DATA_QUALITY_EXCLUDED_TICKERS`
      を10銘柄に拡大（`exclude_data_quality_tickers`は変更なし、定数のみ更新）。
      テスト: `tests/test_layer1.py`
      `test_default_list_matches_check_splits_full_history_findings`
    - **記録: 個別銘柄の性質についての留保（除外の判断には使わない）**:
      6731.T（3日・比率0.5）は分割ではなく実際の株価下落の繰り返しである
      可能性が高い誤検出と見られるが、規則どおり除外する。7877.T・7983.T
      （同日・同比率0.2）は、別会社が同日に同じ分割を行うより、データ
      ソース側の不具合を疑う方が自然だが、これも規則どおり除外する
    - **本番配信（`manual_exclusions.csv`）: 10銘柄すべてに拡大（完了、
      2026-08-28）**。「検証で信用できないと判定した銘柄を配信に出さない」
      という方針により、両窓の除外リストと同じ10銘柄を配信対象からも除外。
      検証側の除外（窓の同一性を守るため）とは目的も寿命も異なる: 本番側は
      全履歴再取得で段差が解消した銘柄から順次解除する時限的な除外、検証側は
      窓を変えない恒久的な除外。`until`は暫定的に2099-12-31（解除は日付では
      なく再取得後の`check_splits_full_history.py`再実行結果で判定するため）
    - **全履歴再取得の恒久的な保守コマンドを追加（完了）**。
      `cli.step_refetch_recent_splits`（新規CLIサブコマンド
      `python -m stockbot.cli refetch-recent-splits`）: storeの直近
      `history_days`本以内にStock Splitsイベントが記録されている全銘柄を
      スキャンし、`history_full_days`ぶん全履歴再取得してstoreへ upsert する。
      `_refetch_new_splits_full_history`（日次fetch中にその場で対応）とは別の
      経路で、storeの現在状態を直接見て能動的に洗い出す。ワークフロー
      `refetch-recent-splits.yml`を追加（`data-daily`concurrencyグループ、
      手動実行）。**実行順序（2026-08-28指示）: チャンク7完了後に実行する
      （チャンク1〜6と7の生成中にstoreの状態を変えないため）。** 実行後、
      `check_splits_full_history.py`を再実行し、段差が消えた銘柄から本番除外
      （`manual_exclusions.csv`）を解除する。3477.T・7877.T・7983.Tは2017年の
      データで`history_full_days`（既定2600本）の外にあり、解消しない見込み
      （頑健性窓の除外は維持）
      テスト: `tests/test_split_refetch.py` `RefetchRecentSplitsFullHistoryTest`
      （直近history_days本以内のSplitsだけを対象にする・無ければno-opの2点）
    - **run #1実行結果（2026-08-28、チャンク7完了後）**: 対象381銘柄
      （`Stock Splits`イベントが直近400本以内に記録されている銘柄。10銘柄の
      除外リストのうち条件に合致したのは**6731.T・9900.Tの2件のみ**——
      1364.T・3477.T・4316.T・5103.T・6834.T・7649.T・7877.T・7983.Tは対象外
      だった。理由: これらはそもそも「Splitsイベントの記録が無い」ことが
      `suspected_unrecorded_split`の定義そのものであり、直近400本以内に
      Splitsマーカーが記録されている銘柄だけを対象にする本コマンドの条件には
      構造的に合致しない。**6834.T・7649.Tが再取得で解消するという見立ては
      検証できていない**（対象外だったため）
    - **check_splits_full_history.py再実行結果: 10銘柄中9銘柄は完全に不変
      （同じ比率・同じ日付）。9900.Tのみ変化**——フラグの日付が2025-01-08
      から2016-02-09（storeの先頭付近）へ移動した。2025-01-08時点の問題は
      解消したように見えるが、2016-02-09で新たにフラグが立っており
      `suspected_unrecorded_split`としては引き続き検出される。**機械的規則
      （個別銘柄の判断はしない）に従うと、9900.Tも含め10銘柄とも本番除外は
      解除しない**（check_splits_full_historyがゼロ件になっていないため）。
      2016-02-09の実態（storeの実際のClose値）は実データで確認中
      （`probe-robustness-start.yml`に確認ステップを追加）
    - **6731.Tは再取得後も不変**。設計責任者の事前の見立て
      （「分割ではなく実際の株価下落の可能性が高い誤検出」）と整合する
      結果（再取得しても直らない＝そもそも分割ではない）
    - **既生成replayとの整合性チェック（結果待ち）**: 全履歴再取得で価格が
      書き換わった381銘柄が、既に生成済みの頑健性窓チャンク1〜7のreplayに
      何行含まれるかを`scripts/count_refetched_ticker_rows_in_replay.py`で
      確認する。0件ならreplayとstoreの食い違いによる実害は無い。非0件なら
      該当銘柄について`verify_full_recompute.py`で一致を確認する
      （比率系特徴量は理論上相殺するはずだが、これまで2回連続で外れた予測と
      同じ形の推論のため、仮説ではなく実測で判断する）
    - **重なりチェックの結果: 289銘柄・24,361行が既生成チャンク1〜7と重なると
      判明**。「重なりゼロなら実害なし、非ゼロなら289件全列再計算」という
      二分は粗すぎるという指摘を受け、非ゼロが意味するのは「storeが一様
      という前提を検証しろ」であって「289件全列再計算しろ」ではないと
      整理し直した。`scripts/scan_window_discontinuities.py`を新設し、
      289銘柄を2016-02-03〜2021-07-31の範囲で`adjust.check_splits`と同じ
      ロジックで段差スキャンし3分類する（全列再計算より桁で軽く、一様性を
      直接測る）: (1)段差が頑健性窓開始日(2017-02-13)以降にある銘柄→両窓・
      ホールドアウトから機械的除外、(2)段差が2016-02-03〜02-08の先頭数行
      だけにある銘柄→該当するチャンク1〜3の行だけ全列再計算、(3)段差なし→
      一様（対照として数銘柄を全列再計算で確認）。同スクリプトは9900.Tの
      実データを使い、2016-02-08がG3(252本)ルックバック窓から外れる最初の
      Tを`idx.get_loc`ベースで実測し、頑健性窓開始日2017-02-13と比較する
      （目算ではなく実測。SMA200=200本は参考値として併記）。
      `probe-robustness-start.yml`に組み込み実行（run #17）。
      **結果（実測、CI実行済み）**: 分類1(段差が頑健性窓2017-02-13以降)=0銘柄、
      分類2(段差が2016-02-03〜02-08の先頭数行のみ)=1銘柄(9900.T、2016-02-09)、
      分類3(段差なし)=288銘柄。**G3(252本)窓の実測は仮説と逆の結果**:
      2016-02-08は2017-02-17にG3窓から外れる——頑健性窓開始日(2017-02-13)より
      「後」であり、「開始日より前で第2分類は空になる」という事前の見立ては
      不成立だった（SMA200=200本の参考値は2016-12-01で、こちらは開始日より
      前）。ただし分類2に該当するのは9900.T一銘柄のみで、9900.Tは既存の
      10銘柄機械的除外規則（`layer1.DATA_QUALITY_EXCLUDED_TICKERS`、
      `exclude_data_quality_tickers`）により両窓・ホールドアウトの集計プールから
      既に無条件で除外されている。この除外は replay 生成時点ではなく
      layer1/layer2 のプール構築時点で適用される関数のため、生の
      `data/replay_robustness` 行としては9900.Tの候補が残り得るが、T-403の
      集計（F1〜F14・ΔF）に使われるプールには到達しない。したがって
      チャンク1〜3の対象行再計算は不要（分類2の実体が「既に除外済みの銘柄」と
      一致したため）。分類1が0件のため新規の機械的除外も不要
    - **分類3(288銘柄・段差なし)の対照チェック**: `verify_full_recompute.py`は
      focus-ticker以外の全銘柄から1行ずつサンプリングする仕様のため、
      `data/replay_robustness`に対して実行すれば288銘柄を横断的にカバーできる
      （個別に数銘柄を選ぶより広い対照になる）。`probe-robustness-start.yml`に
      ステップを追加。**実行結果（CI実行済み、run #20）**: 頑健性窓replay
      （`data/replay_robustness`）に対して9900.T全22行＋他3060銘柄から各1行
      サンプリングで実行。不一致は`[9900.T]`タグの行のみ（9900.T自身の22行、
      既存の除外対象と一致）。**他3060銘柄はサンプリングした1行すべてで不一致
      ゼロ**。分類3(288銘柄・段差なし)を含む頑健性窓全体で一様性の対照確認が
      取れた
    - **構造的修正: `OhlcvStore.upsert_replace`を新設し、全履歴再取得を
      マージから置換に変更（完了）**。9900.Tで2回連続確認された同型の
      構造バグ——固定長ウィンドウ（`history_days`=400、`history_full_days`
      =2600）での再取得をマージすると、ウィンドウの外に残る古い未調整行と
      ウィンドウ内の新しい調整済み行の境界に段差が生じる。ウィンドウを
      伸ばしても再発位置が動くだけで解決しない——への一般的対策。
      対象銘柄については既存の全行を先に削除してから新データを統合する
      ため、fetchできた範囲だけがstoreに残る（取得できなかった先頭数行は
      消えるが、`min_history_bars`=250本の判定があるため実害なし）。
      `_refetch_new_splits_full_history`（`step_fetch`経由）と
      `step_refetch_recent_splits`の両方の再取得経路に適用。
      テスト: `tests/test_store.py::UpsertReplaceTest`（3件: 取得窓外の
      古い行が残らない・対象外銘柄は無傷・対象銘柄リスト空なら通常の
      upsertと同じ挙動）、`tests/test_split_refetch.py`の既存3テストを
      3-tuple戻り値（`refetched`銘柄リストを追加）に合わせて更新。
      全320テスト・DRYRUNとも確認済み
  - **新規Splitsイベント検出時の全履歴再取得を実装（完了）**。9900.Tのケースで
    判明した構造的な問題: 日次fetch（`history_days`、既定400本）の窓内で
    新規に記録された分割（`check_splits`の`kind=="unadjusted_split"`）は、
    その窓の中だけが調整され、窓より過去のstore側の値は未調整のまま残るため、
    窓の境界（400本前後）に段差が立つ。**これは9900.Tに限らず、今後どの銘柄が
    分割しても再発する構造的な問題**（2023年以降の分割増加を踏まえた指摘）。
    `cli._refetch_new_splits_full_history`を追加し、`step_fetch`に組み込んだ:
    新規Splitsイベントを検出した銘柄だけ`history_full_days`（新設定
    `HISTORY_FULL_DAYS`、既定2600本）で全履歴を再取得し、store全体を整合
    させる。`config.Settings`に`history_full_days`を追加（§12のスコアリング
    パラメータではなく、`history_days`と同じ「取得」区分の運用パラメータ）。
    テスト: `tests/test_split_refetch.py`（対象銘柄だけが全履歴再取得される・
    unadjusted_split以外のissueでは発火しない・issueが空ならno-op の3点、
    fetch関数を差し替えてネットワーク無しで検証）
    - **9900.Tには効かなかった（実測、2026-08-28のdata-daily実行ログで確認）**。
      同日発火したのは別銘柄（8227.T）で、機能が動作すること自体は確認できた。
      9900.Tで発火しなかった理由: `check_splits`は「その回の取得窓の中に
      未調整の跳びがあるか」だけを見る、状態を持たない毎回フルスキャンの
      関数。今日の400本窓が既に内部無矛盾（跳びが無い）であれば
      `unadjusted_split`は立たない。段差は窓の外（storeの当該銘柄の過去
      バー、2025-01-08）に残ったままであり、日次fetchの窓では原理的に
      見えない。取得窓は毎日スライドするため、この種の見落としは9900.Tに
      限らず今後も起こり得る（`check_splits_full_history.py`による定期的な
      全履歴スキャンが実質的な補完策になる）
  - **SPEC §4 緩和策3（日次スナップショット）の稼働状況確認: 完了。未実装
    と判明**。`snapshot.yml`・`data/snapshot.py`のいずれも存在せず、
    TASKS.mdにもタスクとして未登録。SPEC.md §4には計画として記載されている
    のみ。今回は`revisions.csv.gz`（別の仕組み。新規取得値と直前のstore値の
    差分記録）で偶然代替確認できたが、本来の緩和策3（時点ごとのスナップショット
    蓄積）は無い状態が続いている

### T-403 L1 集計
- 目的: `validation/layer1.py`: DESIGN.md §10.2 の 0〜6。相関行列、単変量 10 分位曲線、次元 IC（日次 Spearman → 平均・NW t）、総合 V1/V2/V3 の IC・分位曲線・成功率、切り口別（状態・地合い・深さ帯・年・市場区分・月）、ベースライン (a)(b)(c)、検定数ログ、BH-FDR、生存バイアス上限
- 受け入れ: すべての表が csv と markdown で出る。検定数が自動で数えられる
- 注意: 閾値の解釈や採否の判断はしない。表を出すだけ
- **R1.1（R1 §15 の 15/16/20 番）で追加するスコープ**:
  - **§10.2-0 健全性の 2 件の検査**（結果は記録するだけ。採否判断はしない）
    1. L1 の相関表が空だった件を確認する。`depth_pct` / `depth_atr` / `retrace` は
       同じものを測っているはずで |ρ| > 0.7 の組が 1 つも無いのは不自然。相関計算の
       実装（対象列・欠損処理・ペアリング）を検査し、原因を特定して記録する
    2. 検定数の内訳が 38 univariate なのに特徴量が 39 個ある件を確認する。どの特徴量が
       カウントから漏れたかを特定して記録する
  - **ベースライン (a′) の追加**: G0〜G3 を通過しているが押し目状態でない銘柄の等加重。
    (c) − (a) と (c) − (a′) の両方に日次差分の NW t を付ける（点推定だけでは不十分）
    - **2026-08-29 実行条件確認で未実装が判明、実装完了**: 設計責任者から「(a′) が
      PR #48 の報告に含まれていない」との指摘。確認したところ (a)(b)(c) は
      `layer1.py` に実装済みだが (a′) は本当に未実装だった。原因:
      `pipeline.compute_daily_features` は「採点対象」（gate_pass かつ状態が
      形成中/反発開始/ブレイク）以外の行を計算後に捨てるため（§10.2-4(a′) が
      必要とする「gate_pass だが押し目状態でない」母集団は既存の replay 出力
      （data/replay・data/replay_robustness）には最初から存在しない）。
      `validation/replay.py` に `_a_prime_one_day`/`run_a_prime_replay`/
      `load_a_prime_table` を追加: 対象母集団を反転して同じ gate/state 判定
      ロジック（swings→pullback→gates、O(n)のswings検出が支配的というコスト
      構造は compute_daily_features と同等）を独立に再実行し、r_h（h∈H_LIST）
      を日付ごとの別ファイル（data/a_prime、`run_replay`と同じ中断再開規約）に
      保存する。CLIラッパー: `scripts/run_a_prime_replay.py`。
      `layer1.py` に `c_minus_a_prime_series`/`c_minus_a_prime_summary`
      （`delta_f_series`/`delta_f_summary`と同じ日次差分→NW tの形）と
      `select_h`（DESIGN.md §9.3の選択基準「(c)−(a′)が最大になるh」を
      h∈{3,5,10,15,20,30}全てに機械的に適用するだけで、値の解釈・妥当性判断は
      しない）を追加。テスト: `tests/test_replay.py`
      `APrimePopulationTest`（gate_pass=True かつ 状態が非採点対象のときだけ
      含む・状態が採点対象ならゲート判定を待たず除外・gate_pass=Falseなら除外の
      3点）・`RunAPrimeReplayTest`（日次ファイル保存・中断再開・
      `load_a_prime_table`）、`tests/test_layer1.py` `CMinusAPrimeTest`
      （手計算一致・日付の和集合・`select_h`が最大値のhを1つだけ選ぶ・
      全hが計算不能なら選択なし）。全334テスト・DRYRUNとも確認済み
    - **設計窓（2021-08-01〜2024-01-31）での実行: 新規ワークフロー
      `a-prime-design-window.yml`を追加**。
      **チャンク1実行結果（2021-08-01〜2021-12-31、CI実行済み）**: 新規104日・
      81,674行（約785行/日）・合計経過5,181秒（約49.8秒/日）。設計窓は約625
      営業日のため、残り約521日ぶん（チャンク2以降、約5チャンク）が必要。
      チャンク2（2022-01-01〜2022-06-30）トリガー済み
      store・data/replay・data/replay_robustness のいずれにも書き込まない
      別アーティファクト（data/a_prime）。頑健性窓と同じ理由でコストが大きい
      （評価対象銘柄数×日数のオーダーでswings検出が支配的。頑健性窓チャンク8
      の実測=123営業日/約1時間45分から外挿すると設計窓(約625営業日)は複数
      チャンクに分ける必要がある）ため、半年弱単位のチャンクに分けて
      `replay-robustness.yml`と同じ運用（`data-daily`同時実行グループ、
      中断再開）で回す。h の選択（`scripts/select_h.py`）と判定（第1・第2関門）
      は全チャンク完了後に実施する
    - **2026-08-29 指示: 判定（第1関門・第2関門）は h の凍結後まで保留**。
      これまでの ΔF 報告は `DEFAULT_H=10`（実装上の既定値であり設計上の選択では
      ない）で行われており、DESIGN.md §9.3 の手順（設計窓だけで h を選び凍結 →
      その h で第1関門 → その h で第2関門）を踏んでいなかった。(a′) 実装・
      設計窓での実行が完了し h が選択・凍結されるまで、判定は行わない
    - **(a′)設計窓の全6チャンクが完了（CI実行済み）**: 613営業日・506,009行
      （チャンク1: 104日/81,674行、チャンク2: 120日/125,109行、チャンク3:
      124日/199,411行、チャンク4: 122日/325,614行、チャンク5: 124日/483,488行、
      チャンク6: 19日/506,009行）。合計経過約7.3時間（6チャンク合計）
    - **h の選択・凍結（実測、CI実行済み、`scripts/select_h.py`）**:
      設計窓（2021-08-01〜2024-01-31、613日）で (c) pool 177,821行・(a′)
      505,067行を用い、h∈{3,5,10,15,20,30} 全てで (c)−(a′) の日次差分・NW t
      （ラグ=h）を機械的に算出した。

      | h | (c)−(a′) 平均 | NW t | p |
      | - | - | - | - |
      | 3 | 0.000577 | 2.310 | 0.021 |
      | 5 | 0.000540 | 1.311 | 0.190 |
      | 10 | 0.000363 | 0.498 | 0.619 |
      | 15 | -0.000134 | -0.146 | 0.884 |
      | 20 | -0.000915 | -0.807 | 0.419 |
      | 30 | -0.001998 | -1.223 | 0.221 |

      **選択された h = 3**（(c)−(a′) が最大、かつ唯一 t が有意）。h が伸びるほど
      単調に悪化し、h=15 以降は符号が反転する。`layer1.DEFAULT_H` を10→3に
      更新（選択基準の適用結果を反映するだけで、選択基準そのものは変えていない。
      全334テストで h=10 に依存する箇所は無いことを確認済み、DRYRUN・全テスト
      とも再確認）。**以後の全h集計・§10.3の第1関門・第2関門判定はこの h=3 を
      使う。値の解釈（有意性の妥当性、符号反転の意味等）はしない、事前登録済み
      の選択基準を機械的に適用した結果を報告するのみ**
    - **2026-08-29 指示: 判定前の3件対応**
      1. **§10.2手順1の未実施分（F1〜F14の分位境界をh=10で決めていた）**:
         `scripts/recompute_f_conditions_h3.py`を追加。設計窓・h=3で各条件が
         除外する末端分位のmean(r_3)を再集計する（向き・閾値は変更しない、
         §5の用途列は事前登録済みで19件として検定数計上済みのため付け替えは
         しない）。`probe-robustness-start.yml`に組み込み実行、結果は
         ΔFより先にこの節に追記する（次回実行分）
      2. **hの選択がグリッド端点である**: DESIGN.md §10.7に限界として追記済み
         （候補集合{3,5,10,15,20,30}の最小値が選ばれており、単調減少関数に
         対する「最大を選ぶ」規則は必ず端点を返す。h=1・2は新規検定かつ2回目の
         見直しに当たるため追加しない）
      3. **効果量の事前記録**: DESIGN.md §10.7に追記済み（h=3の(c)−(a′)=5.8bp
         は日本株の往復コスト20〜40bpの1桁下。第2関門を通ってもL2で経済性が
         成立しない可能性が高いことを判定前に記録。h=15・h=20での符号反転も
         併記——長い保有では押し目状態が非押し目より劣る可能性を示す）
      4. **チャンク突合**: `scripts/verify_chunk_coverage.py`を追加。設計窓
         (a′)・頑健性窓それぞれで、データ駆動の非取引日判定を適用した期待
         営業日数と実際の保存日数を突合し、Actionsのキャッシュ上書き等による
         静かな欠落が無いかを確認する。`probe-robustness-start.yml`に組み込み
         実行。
         **実行結果（CI実行済み、run #23〜#24）**: 設計窓(a′)は期待613日・
         保存613日で完全一致（不足0・余剰0）。頑健性窓は期待1,111日・
         保存1,135日（不足0・余剰24）。余剰24件（2017-03-20、2019年の改元に
         伴う10連休を含むゴールデンウィーク・年末年始等の実在休場日）は
         全て`n_rows=0, empty_reason=no_data`——**候補行を持つファイルは
         ゼロ、ΔF_tへの実害なし**。ユーザー指摘の「2019年GWがチャンク5生成分
         （非取引日フィルタ導入前）」の懸念は解消
      テスト: `tests/test_recompute_f_conditions_h3.py`（末端分位が負のまま/
      反転/特徴量欠損で判定不能/集計件数一致の4点）、
      `tests/test_verify_chunk_coverage.py`（ファイル名からの日付抽出・
      不足日と余剰日の検出・欠落なしの場合・余剰日が実害あり/なしの判別の
      7点）。全344テスト・DRYRUNとも確認済み
    - **2026-08-29 指示（設計の循環の指摘、設計責任者の誤りとして明記）**:
      F の向き（§5・§6.1、h=10由来）と h の選択基準（§9.3、別基準）が
      独立でなく衝突しうる設計だったことが、実際に10/14条件の反転として
      露呈した。DESIGN.md §10.7に追記済み。**向きの再フィットはしない**
      （見直し1回を消費済み）。F1〜F14はh=10由来の向き・閾値のまま、h=3で
      第1関門（内部確認窓）に進める。依然として負の4条件（F4・F10・F11・
      F12、いずれも「伸びきったものを買わない」形）は「トレンド内の短期の
      伸びきり回避」という別仮説としてDESIGN.md §13.1に記録し、今回はテスト
      しない（新規検定のため別プロジェクトとして未使用データで事前登録し
      直す対象）
    - **設計窓のΔF・押し目プール全体mean(r_h)の報告**: `scripts/report_design_window_delta_f.py`
      を追加。ΔF = mean(F通過) − mean(全押し目)のため、各条件の寄与は
      「末端分位が負か」ではなく「プール平均を上回るか下回るか」で決まる。
      h=3（判定に使う。ただしこれ自体は第1関門ではなく設計窓での確認・記録）
      とh=10（記録のみ、判定に使わない）の両方を出す。
      `probe-robustness-start.yml`に組み込み。
      **実行結果（CI実行済み、run #25）**: 177,821行・613日。
      h=3: 押し目プール全体mean(r_3)=+0.0739%、ΔF_3 mean=+0.0297%
      se=0.0271% t=1.094 p=0.274（n=613/613）。h=10（記録のみ）:
      プール平均mean(r_10)=+0.1963%、ΔF_10 mean=+0.1958% t=2.467
      p=0.0136。**事前予測（負またはゼロ近傍）は外れた**——プール平均を
      下回る方向は当たったが符号は正のまま。第1関門は§10.3どおり内部確認窓
      （2024-02〜2026-01）で判定する（設計窓のt=1.094を理由に中止しない、
      事前登録していない停止規則を追加しないため）
    - **第2関門の具体値を事前登録（2026-08-29、頑健性窓のΔFを見る前に確定）**:
      DESIGN.md §10.3に追記。「設計窓の効果量の1/2以上」=ΔF≥+0.0149%
      （1.5bp、設計窓ΔF_3=+0.0297%の半分）。拘束するのはNW t≥2の方で、
      頑健性窓（n=1,068、密度の低い年を含みばらつきが設計窓より大きい）で
      t≥2に必要なΔFはおよそ+0.041%〜+0.055%と見積もる（目安であり判定
      基準そのものではない。判定は実測値で行う）
    - **第1関門の実行に着手**: 内部確認窓（2024-02-01〜2026-01-30）の(a′)を
      設計窓と同じ`a-prime-design-window.yml`（date range引数は汎用、
      data/a_primeキャッシュは日付ベースで両窓分を同一キャッシュに蓄積できる）
      で計算する。設計窓と同規模（約500営業日、複数チャンク）。
      **全5チャンク実行結果（CI実行済み、いずれも異常なし）**:
      チャンク1（run #7 id=33279924361、2024-02-01〜2024-06-30）新規101日／
      経過5,755秒／累計714日・623,371行。
      チャンク2（run #8 id=33291402714、2024-07-01〜2024-12-31）新規125日／
      経過8,416秒／累計839日・715,829行。
      チャンク3（run #9 id=33297600798、2025-01-01〜2025-06-30）新規119日／
      経過8,011秒／累計958日・794,367行。
      チャンク4（run #10 id=33308806813、2025-07-01〜2025-12-31）新規124日／
      経過5,774秒／累計1,082日・964,494行。
      チャンク5（run #11 id=33314720746、2026-01-01〜2026-01-30、最終）
      新規19日／経過970秒／累計1,101日・993,343行。
      **内部確認窓(a′)の全チャンク完了**（累計値は設計窓613日分を含む。
      内部確認窓単独の新規分＝101+125+119+124+19=488日）。
      `verify_chunk_coverage.py`に内部確認窓（2024-02-01〜2026-01-30）の
      チャンク突合チェックを追加（`data/a_prime`は日付ベースキャッシュで
      両窓を蓄積するため、`check_coverage`の`start<=d<=end`フィルタで
      窓ごとに独立して突合できることを利用）。`probe-robustness-start.yml`に
      `scripts/gate1_internal_confirmation.py`実行ステップを追加、第1関門を
      次回CI実行で判定する
    - **余剰24件の決着（2026-08-30指示、3回目の確認要求への回答）**: 最新の
      `verify_chunk_coverage.py`実行（run #25 id=33278641407、PR#63反映後の
      main HEAD）で頑健性窓の余剰24日付を全件確認。
      `['2017-03-20', '2017-05-03/04/05', '2019-01-01/02/03', '2019-01-14',
      '2019-02-11', '2019-03-21', '2019-04-29', '2019-04-30', '2019-05-01',
      '2019-05-02', '2019-05-03', '2019-05-06', '2019-07-15', '2019-08-12',
      '2019-09-16', '2019-09-23', '2019-10-14', '2019-10-22', '2019-11-04',
      '2019-12-31']`。2019年の改元10連休のうち平日に当たる6日
      （04-29/04-30/05-01/05-02/05-03/05-06）は全てこの余剰リストに含まれて
      おり、**全24件が`n_rows=0, empty_reason=no_data`**（候補行を持つ
      ファイルはゼロ）。2019年GWがΔF_tの時系列に穴を空けている、または
      非取引日フィルタ導入前のチャンク5生成分が汚染しているという懸念は
      いずれも解消——**実害なしを最新mainのCI実行で再確認**。これ以上の
      対応不要、決着済み
    - **内部確認窓の集計スクリプトに窓フィルタを明示（2026-08-30指示）**:
      `scripts/gate1_internal_confirmation.py`を追加。data/a_primeが日付
      ベースのキャッシュで設計窓・内部確認窓の両方を同一アーティファクトに
      蓄積するため、(c) pool（data/replay由来）・(a′)（data/a_prime由来）の
      両方を`INTERNAL_CONFIRMATION_WINDOW=(2024-02-01, 2026-01-30)`で明示的に
      絞り込んでから`layer1.delta_f_summary`・`layer1.c_minus_a_prime_summary`
      を呼ぶ（`select_h.py`・`report_design_window_delta_f.py`と同じ窓フィルタ
      パターン）。第1関門の条件（W1のΔFが正、かつ(c)が(a′)を上回る）を機械的に
      判定して表示するのみで、通過/不通過の意味は解釈しない。
      テスト: `tests/test_gate1_internal_confirmation.py`
      （設計窓の日付が同一フレームに混在していても内部確認窓外の行が除外される
      ことを直接確認する3点：窓混在フレームからの除外、窓末尾より後の日付の除外、
      空フレームの通過)。全347テスト確認済み。(a′)内部確認窓が全チャンク完了
      次第、このスクリプトで第1関門を実行する（まだ未実行、チャンク2以降待ち）
    - **第1関門の判定結果（CI実行済み、run #26 id=33320167759、probe-robustness-start.yml）**:
      内部確認窓 2024-02-01〜2026-01-30: (c) pool 183,819行・488日 /
      (a′) 486,333行・488日。h=3:
      W1 ΔF_3 mean=+0.0616% se=0.0360% t=1.709 p=0.0875（n=479/488）、
      (c)−(a′)_3 mean=−0.0114% se=0.0302% t=−0.379 p=0.7044（n=485/488）。
      条件1（W1のΔFが正）= True。条件2（(c)が(a′)を上回る）= False。
      **第1関門: 不通過**（事前登録済みの機械的規則をそのまま適用した結果。
      DESIGN.md §10.3どおり、解釈・採否の判断はしない）
  - **年別の1日あたり行数（記録用、2026-08-29 指示）**: `scripts/replay_run_summary.py`
    に `by_year_summary` を追加。主評価窓・頑健性窓のどちらの `--replay-dir` でも
    年別の対象日数・延べ行数・平均行数/日を出す（既存の `warmup` 除外ロジックを
    再利用）。地合い（2018年末急落・2020年コロナ）でG2落ちが増え密度が下がるのは
    窓の性質として想定内（頑健性窓をレジーム多様性のために選んだ以上の帰結）。
    採否判断はしない、記録のみ。結果は頑健性窓の次回チャンク実行時に自動で出る
    （`replay-robustness.yml`の summary ステップが呼ぶ）
    - **実測結果（CI実行済み、probe-robustness-start.yml run #20）**:
      主評価窓 2021=104日/242.5行/日、2022=244日/172.8行/日、
      2023=246日/415.7行/日、2024=245日/340.5行/日、2025=243日/403.5行/日、
      2026=19日/598.2行/日。
      頑健性窓 2017=198日/489.8行/日、2018=246日/198.2行/日、
      2019=241日/157.6行/日、2020=242日/187.9行/日、2021=141日/400.4行/日。
      設計責任者の見立て（チャンク5〜7区間≈157行/日）と2019年の実測値157.6が
      ほぼ一致
  - **ΔF の計算**: `ΔF_t = mean(r_h | F 通過, 日 t) − mean(r_h | 全押し目状態, 日 t)` の
    時系列平均と NW t。F1〜F14 の leave-one-out も同じ形式で出す（採否の判断はしない）
    - **F1〜F14 の判定・ΔF_t 集計本体を実装（完了、2026-08-28、PR未マージ時点でこの
      コミットに含む）**: `validation/layer1.py` に `pool_percentile_series`
      （§6.3 のプール内百分位。scoring.composite の`_percentile_up`と同じ定義、
      直近 F_POOL_DAYS=20 営業日・過去方向のみでルックアヘッド無し）・`f_pass_mask`
      （§6.1 の F1〜F14 をそのまま実装。欠損は判定不能=通過扱い、`f_missing_count`
      を持つ）・`delta_f_series`/`delta_f_summary`（NW t は既存の`newey_west_mean_t`
      を再利用、ラグ=h）を追加。テスト: `tests/test_layer1.py`
      `PoolPercentileSeriesTest`（手計算一致・窓外の日を含まない・ルックアヘッド無し
      の3点）・`FPassMaskTest`（上端/下端除外・欠損は除外しないことを確認）・
      `DeltaFSeriesTest`（手計算一致・行数閾値による日除外）。leave-one-out・
      MFE/MAE 表・全h集計は未実装のまま（この節の残タスク）
    - **9900.T のデータ品質除外をコードで実装（完了）**: `layer1.exclude_data_quality_tickers`
      （`DATA_QUALITY_EXCLUDED_TICKERS = {"9900.T"}`）。これまで本番配信側
      （`manual_exclusions.csv`）にのみ実装されていた除外を、L1 集計側にも反映した
    - **押し目状態の行数が閾値未満の日をΔF集計から除外する基準を事前登録
      （2026-08-27、結果を見る前に設計責任者が指定）**: `delta_f_series`/
      `delta_f_summary` の `min_rows_per_day` 引数（既定 None=フィルタ無し、
      診断実行では 30 を使用）。理由: ΔF_t は日次横断平均の時系列平均であり、
      2銘柄しかない退化した日と300銘柄超の日が同じ重みで時系列平均に入ると、
      空日ガードをすり抜けた「幻の1〜2行」の日が結果を歪め得るため。両窓の
      日次中央値が約330件であることから、この閾値は実質的に幻の日だけを
      落とすルールであり、通常営業日を誤って除外する副作用は無い見込み
      **→ 2026-08-28撤回。** 実測（チャンク4時点）で見込みが外れ、休場日の幻の
      行だけでなく市場ストレス期の正当な低候補日も一緒に落としていた上、
      両窓とも除外後にtが上がる（望ましくない自己成就的な効き方）ことが
      判明したため撤回。詳細は本ファイルT-402節の該当箇所を参照。
      `min_rows_per_day`引数自体はコードに残すが（呼び出し側が明示指定しない
      限り無効）、既定の集計では使わない。根本原因（非取引日の混入）は
      `validation/replay.py`の`_real_trading_days`で対応済み
    - **ADV（20日平均売買代金）診断を追加（完了）**: `scripts/robustness_liquidity_diagnostics.py`
      （診断専用・store/replay 変更なし）。pool の各行についてその日までの直近
      20営業日の Close*Volume 平均を因果的に計算し、窓別分布と ADV≥2億円
      部分集合での ΔF を報告する（判定には使わない診断。2026-08-27 チャンク4
      結果を受けた事前登録）
  - **MFE/MAE の表**: 分位別・F 通過別・状態別・深さ帯別に MFE/ATR と MAE/ATR の平均を出す
    （§9.2 で主指標に格上げされたため必須）
  - **全 h の集計**: h ∈ {3,5,10,15,20,30} すべてについて単変量・ΔF・MFE/MAE を出す
    （h=10 だけだった L1 の既定値を解消。h の選択は設計窓のみで行う、§9.3）
  - 頑健性窓（T-402）の replay が生成され次第、上記すべてを頑健性窓に対しても実行し、
    §10.3 第 2 関門の判定材料とする
  - **生存バイアス上限評価は窓別に算出する**（2026-08-26 実行条件の指示）。全体値
    （765/3,711=17.1%）は主評価窓と頑健性窓の効果量比較には使えない。頑健性窓は
    主評価窓の約2倍の年数ぶん上場廃止の累積が効いており、全体値を流用すると
    §10.3 第2関門の「1/2以上」の閾値が通りやすい方向にバイアスがかかる
  - **判定の先取り禁止**（2026-08-26 実行条件の指示）: §10.3 第2関門は全窓の ΔF
    （NW t・効果量）のみで判定する。年別・月別・地合い別・市場区分別の集計は
    頑健性窓でも診断・記録のみに留め、判定には使わない（この点は設計変更ではなく
    §10.3 の既存規定の運用上の確認であり、DESIGN.md は改訂しない）
  - **warmup=True の行は集計から除外する**（頑健性窓のみ。DESIGN.md §10.1 開始日の
    規則。`scripts/replay_run_summary.py` は既定でこれを除外する）

### T-404 西村ルールの再現(較正)
- 目的: DESIGN.md §10.2 (b) のルールをそのまま売買シミュレーションし、2016〜2020 の勝率・平均損益・PF・平均保有を出す
- 受け入れ: 結果が `validation/reports/calibration_nishimura.md` に出る。期待値（勝率 60% 前後、PF 1.5 前後）との差を数値で併記（解釈はしない）

### T-405 L1 レポート
- 目的: `validation/report.py`: T-403 の表と図（分位曲線、IC 時系列、地合い別）を 1 つの markdown に束ねる。冒頭に「検定数」「ホールドアウト未使用」を明記
- 受け入れ: Fable がこのレポートだけで判定できる（DESIGN.md §10.3 の項目がすべて載っている）

---

## 判定（Fable が行う。実装者は待つ）
L1 レポートを受け取り、継続 / 見直し 1 回 / 撤退 を決める。見直しの場合は DESIGN.md を改訂し、検定数を引き継ぐ。

---

## M3b スコア表示（L1 通過後）

### T-311 判定キット
- 目的: 押し目候補 40〜60 枚のチャート画像（銘柄名を伏せ、T 以降を隠す）を、深さ帯・地合い・業種が偏らないよう抽出して出力。判定結果（良/普通/悪）を csv で受け取り W を校正
- 受け入れ: 校正前後の L1 IC を並べて出す。校正は各セル ±0.25 まで

### T-312 内訳と自動文・多様性
- 目的: DESIGN.md §8.2〜8.3。順位付け、業種上限、同テーマ判定、カード内訳、一言
- 受け入れ: 合成データで上位 10 に同業種が 3 を超えない

---

## M4-L2 検証レイヤー 2（L1 通過後）

**2026-08-30: 以下 T-411〜T-413 は実施せず。** DESIGN.md §10.3 第1関門
（内部確認窓）が不通過となり、プロジェクトが事前登録済み停止規則により終了した
ため（`CLOSING.md`）。第2関門以降に到達していない。

### T-411 売買シミュレータ（実施せず）
- 目的: DESIGN.md §10.4。エントリー A/B、出口 ①〜④、1 単位等金額、同時保有上限 10
- 受け入れ: 小さな手計算例と一致。B の未約定が集計される
- 必須テスト: 出口の優先順位（同日にストップと利確なら損失側）

### T-412 指標とレポート（実施せず）
- 目的: 勝率、PF、期待値 R、平均保有、最大 DD、日次シャープ、回転率、Deflated Sharpe。年別・地合い別
- 受け入れ: markdown レポート。パラメータ格子の「効く範囲」が表で見える

### T-413 ホールドアウト適用（実施せず）
- 目的: Fable が指定した最終構成を 1 回だけホールドアウトに適用
- 受け入れ: 実行は 1 回。結果と構成を `validation/reports/holdout.md` に固定
- **2026-08-30: ホールドアウト（2026-02〜08）は未使用のまま保全する。削除しない**

---

## M5 運用化

**2026-08-30: 以下 T-501〜T-505 は実施せず**（理由は上記 T-411〜T-413 と同じ）。
ただし `data-daily`（`.github/workflows/data.yml`、日次 OHLCV アーカイブ）は
このプロジェクトの判定と独立であり、前向き検証（DESIGN.md §10.5）用のデータ
蓄積として停止しない。

### T-501 描画（実施せず）
- 目的: `render/`: 既存インフォグラフィックのデザイン（hero.jpeg、mascot.png、ヘッダー帯、日付バッジ、地合いボックス、データ状況、番号付きカード、監視リスト、免責、まとめ画像）を HTML テンプレート（Jinja2）で再現し、headless Chromium（Playwright）で PNG 化。中身は DESIGN.md §8.3
- 受け入れ: 合成データで 2 枚の PNG が生成される。日本語フォント（Noto Sans CJK）で崩れない。Pillow 版は作らない
- 注意: 画像 2 枚（IMG_7291/7292）が参照デザイン。配色・余白・角丸・バッジ色を踏襲

### T-502 配信（実施せず）
- 目的: 既存の `line_send.py`（Worker + R2 経路）を `notify/` に移設して使う。Worker 側（`src/worker.js`）は変更しない
- 受け入れ: `LINE_DRY_RUN=1` で payload が表示される。画像 2 枚 + 失敗時テキストのフォールバック

### T-503 ワークフロー統合（実施せず）
- 目的: `.github/workflows/screen.yml`: data-daily の後段にスクリーニング・描画・配信を追加。cron 7:00 JST。失敗は赤にする
- 受け入れ: workflow_dispatch の dryrun で全段が通る

### T-504 前向き検証の突合（実施せず）
- 目的: `validation/resolver.py`: 日次で h 日前の特徴量ファイルに実現ラベルを付ける。月次レポート（IC・成功率・分位差 vs バックテスト期待値）
- 受け入れ: 欠損日があっても落ちない。レポートは markdown

### T-505 監視規則（実施せず）
- 目的: 6 か月移動 IC が負なら配信を「監視のみ」に切り替えるフラグと通知
- 受け入れ: 合成で規則が発火する

---

## 横断

### T-001 テスト規約
- `tests/` は unittest 互換。ネットワーク不要。`SCREEN_DRYRUN=1` で全工程が通ること

### T-002 CI
- push と PR で全テスト。`data-daily` とは別ワークフロー

---

## やってはいけないこと（全タスク共通）
- 未来の行を参照する計算（スイングの未確定利用、T を含む週の週足、T+1 以降の価格）
- DESIGN.md §12 にないパラメータの追加、∩ の帯や重みの変更
- 変種 V1/V2/V3 以外のスコア方式の追加
- LINE 経路（`src/worker.js`、`wrangler.toml`、Secrets 名）の変更
- 保存データ（`data/store/`）の git コミット
- ホールドアウト期間のデータを L1 / 設計の途中で見ること
- 検証結果の解釈・採否判断（Fable が行う）

---

## 質問ログ（実装者が追記）

| 日付 | タスク | 質問 | 回答 |
|---|---|---|---|
| 2026-08-22 | T-202 | §3の交互化規則「同種スイングが連続したら、より極端な方を残す（後から来た方が極端なら置き換える）」と、§11の必須テスト「Tで切った検出＝全期間検出をi≤T−kで絞ったもの」が数学的に両立しません。証明: スイング高値の条件は `High[i] > max(High[i+1..i+k])`（右側は厳密不等号）なので、あるi2がi1より極端（High[i2]>High[i1]）になり得るのは必ず i2 > i1+k の場合のみ（i2≤i1+kならi1自身の条件からHigh[i2]<High[i1]が強制される）。よって「置換」が発生する全てのケースでi2はi1の確定ラグ窓の外にある。T=i1+k時点で「Tで切った検出」を行うとi2は未来なので結果は{i1}のまま。一方「全期間検出」ではi2がi1を置換するため交互化結果からi1が消え、「i≤T−kで絞る」も空集合になる。{i1}≠{}で必須テストが構造的に成立しない。k=3という既定値では「3営業日超の間隔」は日常的に起こるため、これは稀な境界ケースではなく置換が起きる全ケースで発生する。候補: (a) 文字通り「常に置換」を実装する（§11テストはこのケースで失敗し得る＝マージ不可になる）。(b) 一度確定（i+k）したスイングはその後の同種候補で置換しない、いわば「同種ランは最初に確定したものが勝つ」（§11テストは満たすが「より極端な方を残す」という記述と矛盾する）。(c) 確定インデックスを「同種ランが対向スイングの確定で閉じた時点」まで遅らせ、置換の可能性が尽きてから初めて確定として公開する（TASKS.mdのT-202が返り値に「確定インデックス」を独立列として求めている点と整合するが、DESIGN.md §3には明記が無い追加ルールになる）。どれを正としますか。 | 案(a)「常に置換」を採用。§11の「Tで切った＝全期間で計算してT行を取り出す」は「一括計算して行を取り出す」ではなく「各Tについてそのときまでのデータでローリング計算する」ことを指すと明確化した（DESIGN.md §11冒頭に追記）。交互化は「T時点で確定済み(i≤T−k)の生スイング集合」に毎回適用する純粋関数であり、Tが進んで末尾が置き換わるのは新情報による更新であって未来参照ではない。置換が起こるのは常に末尾の1要素のみ（反対側のスイングjはj+k<i2+kなのでi2より先に確定し、末尾から外れているため）。§3・§11をこの内容で更新済み。(b)は§4.1のH0定義（脚の最高値）と矛盾し有効な押し目を落とすため不採用。(c)は確定ラグを可変にしてしまい、ZigZag排除の理由と同じ問題を持ち込むため不採用。|
| 2026-08-23 | T-302 | §7の出来高テンプレートに理想経路の具体的な数値アンカーがありません。価格テンプレートは「y=1からy=0.6〜0.67へ」「最後の2列は水平」など具体的な数値が明記されており実装できましたが、出来高テンプレートは「同じ格子で、縦軸をVol/mean(Vol[l0..h0])の0〜3の10等分。上昇脚で高く、押し目で減衰する経路に+、押し目で増える領域に−」という定性的な記述のみで、上昇脚の理想経路がVol/mean比率でどの値域を狙うか、押し目の減衰先がどの値域か、「最後の2列は水平」に相当する規則があるか、が書かれていません。CLAUDE.mdの「パラメータを増やさない・新しい閾値が欲しければ質問ログへ」に従い、これらの数値を推測で埋めずに止めています。候補: (a) 価格テンプレートと同型（上昇脚は対角0→ある上限値、押し目は上限値からある下限値へ緩やかに下降し最後の2列水平）とし、上限・下限の具体的なVol/mean比率をご指定いただく。(b) 経路を厳密に指定せず「上昇脚列の平均が押し目列の平均を上回っていること」のような列単位の緩い順序制約に単純化する（W行列のセル単位ではなく列平均の比較にする、DESIGN.mdの「同じ格子」という前提から外れる可能性あり）。(c) 出来高テンプレートはT-302の対象から外し、別タスクとして分離する（d3_templateは当面「価格テンプレートのみ」として実装するか、T-302全体をT-302a価格/T-302b出来高に分割）。どれで進めますか。数値アンカー案(a)を採用する場合は具体的な比率もご指定ください。実装状況: `scoring/template.py`に価格テンプレート（格子化・P・W・適合度）のみ実装済み、テスト（列ごとのPの合計が1・再計算一致・理想経路>崩れた経路）は全て通過。出来高テンプレートとd3_templateの配線（features/dimensions.py）は未着手・未コミットのまま待機しています。 | 案(a)を採用。出来高は縦軸 v=Vol[t]/mean(Vol[l0..h0]) を0〜3の10等分（幅0.3）。理想経路W_vol: 上昇脚の列（0〜a）はv=1.0〜1.4の帯（行3〜4）に+1・隣接（行2、行5）に+0.5。押し目の列（a〜1）のうち最後の1列を除く分はv=0.5〜0.8の帯（行1〜2）に+1・隣接（行0、行3）に+0.5、かつ同じ列でv>1.5（行5以上）のセルに−1。押し目の最後の1列は加点も減点もしない（全セル0）。適合度の計算式は価格と同じ。d3_template=(価格の適合度+出来高の適合度)/2。DESIGN.md §7・§12に追記済み。|
