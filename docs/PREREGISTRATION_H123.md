# 仮説 1・2・3 の事前登録（実運用の記録で確かめる）

**確定（2026-09-19・設計責任者）。この時点で実運用の結果は 1 件も見ていない。**

`docs/BACKTEST.md` §13.11 の仮説 1・2・3 を、実運用の記録で確かめるための事前登録。
**記録を見る前にここを確定させた。順序を逆にしたら、この文書は無効である。**

## 0.1 確定事項（2026-09-19・設計責任者）

| 項目 | 確定内容 | 節 |
| --- | --- | --- |
| ばらつき σ | **実測する**（仮定のままにしない）。実測値は §2.2 | §2.2 |
| σ が仮定より大きかった場合 | **必要件数を引き上げて先送り。基準は緩めない。** ただし到達見込みが 2 年を超えるなら H1 も「判定しない」に落とす | §4.1-2 |
| H1 の登録件数 | **Q5 ≥ 40 かつ Q1〜Q4 ≥ 400** | §2.3 |
| H1 の判定期限 | **2027-03-31**（根拠 4 点） | §3.2 |
| H2 | **判定しない。** 記述として残すだけ | §2.5 |
| H3 | **判定しない。** Q5 限定でも登録しない | §2.5・§4.1-4 |
| 判定の回数 | **1 回だけ** | §5 |
| 検定数 | **15 → 16**（H1 のみ） | §6 |

## 0. 位置づけ

§13 は記述統計であって判定ではない（§13.1）。仮説 1・2・3 は**探索窓・確認窓・
ホールドアウトの 3 窓すべてから出たが、3 窓とも既に見た窓である。再現ではなく、
同じデータの別の切り口**（§13.11）。確かめる場は実運用の記録だけで、これは
バックテストとは独立した標本であり生存バイアスも無い（§12.10）。

**この文書で決めるのは判定の手続きだけ。** 配信・判定式・閾値・並び順は変更しない。

| 項目 | 現状 |
| --- | --- |
| 実運用の累計 | **158 件**（パターン期・2026-09-14〜09-18 の 5 配信日） |
| 結果付け済みの記録 | **0 件**（`pattern_outcome_*.csv` はまだ 1 本も無い・§7.3） |
| 最初の結果付け | **2026-10-15 前後**（概算・§7.3） |
| 検定数 | **15 件**（§12.6） |
| 配信 | 継続中。記録は日々増える |

**この文書の作成時点で運用の結果（損益）は 1 件も見ていない。** 見たのは群分けに使う
配信時点の値（`breakout_h`・`close_t`・`pattern`）の分布（§2.1）と、**§13 の成果物
（バックテストの標本）から実測したばらつき**（§2.2）だけである。

## 1. 測る量（仮説ごとに 1 つ）

3 つとも **§13 で見た量と同じもの**を使う。出口の定義も §13（§10・§11・§12）と
同一にする。**運用のために定義を作り直さない。**

### 1.1 共通の出口定義（§10.2 の `simulate_exit` と同一）

| 項目 | 定義 |
| --- | --- |
| エントリー | `Open[T+1]`（翌営業日の寄付き） |
| 評価窓 | T+1..T+20（営業日） |
| 利確 | `High > target` の日に `target` ちょうどで約定（**strict**） |
| 撤退 | `Low < pattern_low` の日に `pattern_low` ちょうどで約定 |
| 同日に両方 | **撤退**（保守的。`success` の流儀と同じ） |
| どちらも無し | 窓の最後の終値で手仕舞い |
| 損益 | `pnl_pct = (約定価格 / Open[T+1] − 1) × 100`（%） |
| 打ち切り | `n_bars < 20` の行も**母数に入れる**（§13 と同じ）。件数は併記する |
| 手数料・スリッページ | 考慮しない（§10 の理想約定） |

**ATR×2.0 の出口**（仮説 3 用）は利確水準だけを `Open[T+1] + 2.0 × ATR14[T]` に
差し替える。到達判定は `High >= 水準`（**non-strict**。§10.2 B と同じ）。撤退・
時間切れ・約定の扱いは上表と同じ。

### 1.2 群分け（**両方とも固定値。運用の記録で切り直さない**）

| 軸 | 値 | 境界 | 出どころ |
| --- | --- | --- | --- |
| 抜け幅分位 | `b = breakout_h` | 0.1 / 0.243012 / 0.474567 / **0.957142** | `data/reference/pattern_quantile_edges.json`（§4.2） |
| 株価帯 | `close_t` | 1,511 円 / 2,706.67 円 | `data/reference/pattern_price_band_edges.json`（§13.2） |

Q5 の下限 `b ≥ 0.957142` が仮説 1・2 の対象群である。

### 1.3 仮説ごとの測る量

| 仮説 | 測る量（1 つ） | §13 での対応 |
| --- | --- | --- |
| **H1** | 現行基準の**平均損益**。Δ = mean(Q5) − mean(Q1〜Q4) | §13.9・§13.10 の「平均損益」列 |
| **H2** | 現行基準の**平均損益**を Q5 の中で株価帯別に 3 つ | §13.11 仮説 2 の表 |
| **H3** | **同一行のペア差** d = ATR×2.0 の pnl_pct − 現行の pnl_pct の平均 | §13.9・§13.10 の 21 セル比較 |

**他の量は判定に使わない。** 到達率・撤退率・勝率・NW t・MFE/MAE は**併記するが
判定基準には入れない**（§13.4 と同じ扱い。NW t は参考）。

## 2. 判定に必要な件数

### 2.1 いま何件あるか（配信時点の値だけで数えた。損益は見ていない）

158 件の内訳:

| 抜け幅分位 | 件数 | | 株価帯 | 件数 | | Q5 の帯別 | 件数 |
| --- | ---: | --- | --- | ---: | --- | --- | ---: |
| Q1 | 58 | | 低位 | 42 | | 低位 | **5** |
| Q2 | 57 | | 中位 | 35 | | 中位 | **0** |
| Q3 | 24 | | 高位 | 96 | | 高位 | **3** |
| Q4 | 11 | | | | | | |
| **Q5** | **8** | | | | | | |

`b ≥ 1`（既に測定目標に到達済み）は 8 件で、Q5 と同数である。

| 配信日 | 件数 | Q5 | Q5 の割合 |
| --- | ---: | ---: | ---: |
| 2026-09-14 | 13 | 1 | 7.7% |
| 2026-09-15 | 25 | 3 | 12.0% |
| 2026-09-16 | 23 | 2 | 8.7% |
| 2026-09-17 | 37 | 1 | 2.7% |
| 2026-09-18 | 60 | 1 | 1.7% |

**配信件数は増えているが、Q5 の絶対数は 1 日 1〜3 件で頭打ちである。**
**運用の Q5 比率は 5.1%（8/158）で、バックテストの 18〜20% より低い**（§2.2c）。

### 2.2 ばらつきの実測（§13 の成果物・run 35401511557）

**当初の仮定は σ = 10pt だった。実測は 6.1〜8.9pt で、仮定より小さい。**
効果量も、H1 は仮定 4.0pt に対し実測 5.5〜7.0pt と大きく、**必要件数は仮定より
減る**。一方 H3 は §10.4 の「+0.11%」を差と読み違えていたのが誤りで、**実際の差は
+0.97〜+1.14pt、ただし運用の分位構成に直すと +0.17〜+0.42pt**（§2.2c）。

`data/pattern_exit/pattern_strata_*.csv.gz`（Actions 成果物・リポジトリには無い）の
行単位の値から算出した。**これはバックテストの標本であって、これから使う実運用の
記録ではない。事前登録の順序は壊れていない。**

#### a. H1 の実測（現行基準の pnl_pct）

| 窓 | Q5 件数 | Q5 平均 | Q5 σ | Q1〜Q4 件数 | Q1〜Q4 平均 | Q1〜Q4 σ | Δ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 探索窓 | 8,705 | −5.30 | 7.54 | 34,757 | +0.19 | 6.07 | **−5.49** |
| 確認窓 | 7,537 | −5.73 | 8.37 | 29,753 | +0.04 | 6.53 | **−5.78** |
| ホールドアウト | 867 | −6.75 | 8.86 | 3,929 | +0.25 | 7.12 | **−7.00** |

#### b. H3 の実測（ペア差 d = ATR×2.0 − 現行）

| 窓 | 全行の差 | σ_d | Q1 | Q2 | Q3 | Q4 | **Q5** |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 探索窓 | +1.019 | 5.80 | −0.198 | −0.071 | −0.049 | +0.086 | **+5.318** |
| 確認窓 | +1.139 | 6.42 | −0.148 | −0.144 | +0.007 | +0.035 | **+5.875** |
| ホールドアウト | +0.968 | 6.82 | +0.325 | +0.167 | −0.011 | −0.410 | **+5.258** |

**差は Q5 に集中している。Q1〜Q4 は ±0.41pt 以内で、符号も窓で揃わない。**

#### c. 運用の構成に直した H3 の期待差

バックテストの Q5 比率は 18〜20%、運用は 5.1%（§2.1）。§2.2b のセル平均を運用の
分位構成で重み付けし直すと:

| 窓 | 運用構成での期待差 | 同 σ |
| --- | ---: | ---: |
| 探索窓 | **+0.169** | 4.27 |
| 確認窓 | **+0.195** | 4.65 |
| ホールドアウト | **+0.415** | 5.32 |

### 2.3 必要件数（実測で置き換えたもの）

α = 0.05 両側・検出力 0.80 を基準にする。**保守側（効果量は最小、σ は最大）を採る。**

| 仮説 | 効果量 | σ | 計算 | 必要件数 |
| --- | ---: | ---: | --- | --- |
| **H1** | 5.49 | Q5 8.86 / Q1〜Q4 7.12 | 運用比 1:18.75 で SE = 9.01/√n₅、n₅ ≥ (2.802×9.01/5.49)² | **Q5 ≥ 22**（検出力 0.90 なら 29） |
| **H2** | 帯の順序 | — | 3 セルとも要る | **Q5 の 3 帯すべて ≥ 20** |
| **H3（全行）** | 0.169 | 4.27 | n ≥ 7.849 × 4.27² / 0.169² | **約 5,000** |
| （参考）H3 を Q5 に限ると | 5.258 | 11.52 | n₅ ≥ 7.849 × 11.52² / 5.258² | Q5 ≥ 38 |

**登録値（確定・2026-09-19 設計責任者）**

| 仮説 | 登録する必要件数 | 上振れの根拠 |
| --- | --- | --- |
| **H1** | **Q5 ≥ 40 かつ Q1〜Q4 ≥ 400** | 運用の効果量がバックテストより 27% 小さい（Δ = 4.0pt）場合でも検出力 0.80 を保つ。Δ = 5.49 なら 0.97 |
| **H2** | （判定しない・§2.5） | — |
| **H3** | （判定しない・§2.5） | — |

### 2.4 到達の見込み

Q5 の累積は **1.0〜1.6 件/配信日**（§2.1。直近 2 日は 1.0）。配信日は月 20 日前後。

| 仮説 | 必要件数 | 配信日数 | 結果付けラグ（20 営業日） | 最短 |
| --- | ---: | ---: | --- | --- |
| **H1** | Q5 40 | 25〜40 日 | +1 ヶ月 | **2026-12 下旬〜2027-01 中旬** |
| **H2** | 中位 Q5 20 | — | — | **見込みなし**（5 配信日で 0 件） |
| **H3（全行）** | 5,000 | 83〜125 日 | +1 ヶ月 | **2027-03〜2027-05** |

### 2.5 扱い（確定・2026-09-19 設計責任者）

| 仮説 | 扱い | 理由 |
| --- | --- | --- |
| **H1** | **判定する。** 検定 1 件 | 実測の効果量が大きく（Δ 5.5〜7.0pt）、Q5 40 件は期限内に届く |
| **H2** | **判定しない。** 記述として残すだけ | 中位が 5 配信日で 0 件。届く見込みを語る段階にない |
| **H3** | **判定しない。** 検定を増やさない | **理由は「到達不能」ではない。** 運用の分位構成では期待差が +0.17pt まで落ち、約 5,000 件が要る（到達は 2027-03〜05 の見込み）。**H1 の期限（§3.2）内に届かない**、というのが理由である |

#### H3 の「判定しない」について残しておくこと

**当初の草案は「必要件数 約 21,000 件＝到達不能」と書いていたが、これは誤りだった。**
§10.4 の「+0.11%」は ATR×2.0 の平均損益そのものであって現行との差ではない。実測の
差は +0.97〜+1.14pt で、運用構成に直すと +0.17〜+0.42pt である（§2.2b・§2.2c）。
**必要件数は 21,000 ではなく約 5,000 で、到達不能ではなく期限に間に合わないという
理由に改めた。**

**Q5 に限れば 38 件で足りる**（§2.3 の参考行）。**この計算は判断材料として残す。**
ただし今回は登録しない（§4.1-4）。**H1 の判定が終わったあと、運用の Q5 の記録が
貯まった時点で、改めて登録する余地がある。そのときは新規の検定として数える。**

**件数が足りないまま判定するくらいなら判定しない。** 足りない標本で出した判定は、
§13 の記述統計を「確かめた」ことにしてしまう。それが一番避けたい失敗である。

## 3. いつ判定するか

**件数が主、日付は従。**

1. **件数条件（§2.3）を満たした最初の月次確認日に判定する**
2. **§3.2 の期限までに満たさなければ、判定しない**（基準を緩めない）

### 3.1 月次確認（**結果を見ない**）

毎月第 1 営業日に、**群ごとの結果付け済み件数だけ**を数える。**損益・到達率・
勝率は見ない。** 件数の確認は「判定の回数」に数えない（§5）。

**件数を見てから基準を動かさない。** §2.3 の必要件数は、この文書の確定をもって
凍結する。

### 3.2 H1 の判定期限: **2027-03-31**（確定・2026-09-19 設計責任者）

**この日までに Q5 が 40 件に届かなければ、H1 は「判定しない」で閉じる。**
期限を延ばさない。無期限に待たない。根拠は 4 つ。

1. **最短見込みの約 2 倍の余裕がある。** §2.4 のとおり最短は 2026-12 下旬〜2027-01
   中旬。Q5 の累積率が半分（0.5 件/配信日）に落ちても、80 配信日 ≈ 4 ヶ月に結果付け
   ラグ 1 ヶ月を足して 2027-02 下旬には届く
2. **日本株の年度末である。** またぐと決算期とユニバースの入れ替えで配信母集団が
   変わり、事前登録が想定した母集団と別物になる
3. **Q-4 が動けば §4.2 で無効になる。** 待つ期間が延びるほどその確率が上がる
4. **配信件数の増加は Q5 の増加ではない。** 5 配信日で配信は 13→60 件に増えたが
   Q5 は 1〜3 件のまま。待てば届くという前提そのものが確かではない

## 4. 満たさなかったときにどうするか

**仮説ごとに、いまここで決める。**

| 仮説 | 満たさなかった / 判定しない場合 | 満たした場合 |
| --- | --- | --- |
| **H1** | **Q5 の扱いを変えない。Q-4 は未決のまま。** §13.11 仮説 1 は「バックテスト内の記述」に留め、以後の設計根拠にしない | **それでも配信は自動では変えない。** Q-4 の判断材料が 1 つ増えるだけで、Q-4 自体は別に決める |
| **H2** | **帯別の扱いを作らない。** 帯に依存する閾値・許容幅を入れない（±1.5% の ATR 連動化はしない・CLAUDE.md） | （判定しないので該当なし） |
| **H3** | **出口は現行のまま。** ATR×2.0 は §10 の判定（基準未達）に据え置く | （判定しないので該当なし） |

**共通**: **「満たさなかった」は反対の主張の根拠にならない。** 標本不足で判定しな
かった場合はなおさらで、「Q5 は悪くない」とも「ATR×2.0 は劣る」とも言えない。

### 4.1 未決（**確定前に設計責任者の判断が要る**）

1. ~~σ を仮定のままにするか、実測してから確定するか~~ → **実測した**（§2.2・
   設計責任者・2026-09-19）
2. ~~σ が仮定より大きかった場合の扱い~~ → **必要件数を引き上げて先送りする。基準は
   緩めない**（設計責任者・2026-09-19 確定）。**ただし引き上げた結果、到達見込みが
   2 年を超えるなら、H1 も「判定しない」に落とす。先送りと不可能は区別する**
3. ~~H2 を「判定しない」で確定してよいか~~ → **判定しないで確定**（設計責任者・
   2026-09-19）
4. ~~H3 を Q5 に限った形で登録するか~~ → **登録しない。「判定しない」で確定。
   検定数は 16 のまま**（設計責任者・2026-09-19）。理由は 2 つ。

   - **言い換えだから。** §13.11 の仮説 3 は「21 セル全部で現行を上回る」で、そこが
     目の付け所だった。Q5 に絞れば必要件数が 38 件に落ちるのは、Q5 の差が +5.3pt と
     突出しているからである。**数字を見てから、届く形に仮説を切り直している。
     事前登録の順序が壊れる**
   - **H1 と中身が重なるから。** H1 は「Q5 に負が集中する」、Q5 限定の H3 は「Q5 では
     ATR×2.0 が現行を上回る」。**どちらも Q5 の損が大きいという同じ事実を見ており、
     独立した 2 件として数えるのは無理がある**

### 4.2 この事前登録が無効になる条件

次のどれかが起きたら、**この文書は無効になり、判定前に再登録が要る**。

- **Q-4 が動いて「既に測定目標に到達済み」を配信から外した** —— Q5 の母集団その
  ものが消えるので H1・H2 は測れなくなる（`docs/PATTERN.md` §4 Q-4）
- 抜け幅の分位境界・株価帯の境界を切り直した
- 評価窓（20 本）・エントリー（`Open[T+1]`）・同日撤退の扱いを変えた
- 配信の判定式・閾値・並び順を変えた
- データ品質の除外リストの運用を変えた

## 5. 判定の回数

**1 回だけ。中間では見ない。**

複数回見るなら α の調整が要り、検定数と判定基準の両方を動かすことになる。**1 回に
限れば調整が要らない。** §12.7 の「ホールドアウトの使用はこれ 1 回」と同じ流儀。

月次のカウント確認（§3.1）は**結果を見ないので回数に数えない**。

## 6. 検定数（確定・2026-09-19 設計責任者）

| 内訳 | 件数 |
| --- | ---: |
| 既存（§12.6） | 15 |
| H1 | **+1** |
| H2（判定しない） | +0 |
| H3（判定しない） | +0 |
| **合計** | **16** |

**H3 を Q5 限定で登録すれば 17 になるが、登録しないことで確定した**（§4.1-4）。
H1 の判定後に H3 を改めて登録する場合は、**そのとき新規の検定として数える**（§2.5）。

**`N_TESTS_TOTAL` を 15 から 16 に上げるのは判定を実施する時点で、テストで固定する。**
この文書の確定だけでは動かさない（実装は別）。

## 7. 記録の取得方法

### 7.1 足りるもの

| 要るもの | どこから | 状態 |
| --- | --- | --- |
| 群分け（`b`・`close_t`） | `delivered_*.csv` の `breakout_h`・`close_t` | **有る** |
| 出口の水準（`target`・`pattern_low`・`atr_t`） | `delivered_*.csv` | **有る** |
| 現行基準の損益 | `pattern_outcome_*.csv` の `entry_open`・`reached_target(_day)`・`broke_stop(_day)`・`ret_h` | **有る（厳密に再構成できる）** |
| 打ち切り | `pattern_outcome_*.csv` の `n_bars`・`censored` | **有る** |

現行基準の損益は次で再構成できる。§1.1 の定義と一致する。

- `reached_target` が `broke_stop` より先 → `target / entry_open − 1`
- `broke_stop` が先、または同日 → `pattern_low / entry_open − 1`
- どちらも無し → `ret_h`

### 7.2 足りないもの（**H3 を判定する場合に限り**）

**ATR×2.0 の損益は `pattern_outcome_*.csv` だけからは出せない。** `Open[T+1] +
2.0 × ATR14[T]` に**最初に届いた日**の列が無く、`broke_stop_day` との前後を決め
られないため。`mfe_atr ≥ 2.0` は「窓のどこかで届いた」しか言えない。

| 案 | 内容 | 記録の変更 |
| --- | --- | --- |
| (a) | `pattern_resolver` に「`High >= Open[T+1] + 2.0 × ATR14[T]` の最初の日」の列を足す | **要る** |
| (b) | store の四本値から `pattern_exit.exit_one` で事後に計算する | **要らない** |

**§2.5 のとおり H3 を判定しないなら、どちらも不要である。実装はこの文書の範囲外。**

### 7.3 結果付けはいつから・どの経路で動くか

**現状 `pattern_outcome_*.csv` は 0 件である。** 貯まっているのは配信記録だけで、
**判定に使える記録はまだ 1 件も無い。**

| 項目 | 内容 |
| --- | --- |
| 経路 | `data.yml` の **Resolve delivered records** ステップ（`python -m stockbot.cli resolve`） |
| 頻度 | 毎営業日 7:00 JST（`cron: 0 22 * * 0-4`） |
| 分岐 | `resolver.resolve_pending` が `is_pattern_record` でパターン記録を見分け、`_resolve_pattern_file` に回す（評価窓 20 本・`pattern_resolver`） |
| 条件 | **`asof` から 20 営業日が経過したファイルだけ**に結果を付ける。経過前は持ち越し |
| 出力 | `data/daily/pattern_outcome_<配信日>_asof<判定日>.csv` |
| 保存 | `data.yml` の Commit snapshots が `data/daily` を commit / push する |
| 最初の 1 本 | `delivered_2026-09-14_asof2026-09-11`（13 件）の 20 営業日後 ≈ **2026-10-15**。書かれるのは翌営業日の実行（**2026-10-16 前後**） |

2026-10-15 は祝日（09-21・09-22・09-23・10-12）を除いた暦での概算である。store の
営業日軸で数え直すと数日ずれうる（§13.11 の「2026-10-12 前後」と同じ範囲）。

**判定の前に、最初の `pattern_outcome_*.csv` が実際に生成されたことを確認する。**
生成されていなければ件数の議論そのものが成立しない。

**差分外の食い違い**: `data.yml` の Resolve ステップのコメントは「**パターンの記録は
飛ばす**（成功の定義が未決。docs/PATTERN.md §4 Q-3）」と書いてあるが、これは Q-3 が
クローズする前の古いコメントで、コードは `_resolve_pattern_file` でパターンを処理
する。**記録の経路を読むときに誤解を招く。**

### 7.4 判定時に確認すること

- **データ品質の除外 12 銘柄**: 運用ユニバースが除外を見るようになったのは
  2026-09-17 なので、**2026-09-14〜09-16 の配信行に該当があれば §13 と同じく落とす**
- **重複**: 1 銘柄が複数パターンで配信されうる。キーは `ticker` + `asof` +
  `pattern` で、§13 と同じく**行単位で数える**
- **打ち切り**: `n_bars < 20` の行も母数に入れ、件数を併記する（§1.1）

## 8. この文書の扱い

- **確定したら凍結する。** §2.3 の必要件数・§3.2 の期限・§4 の対応・§5 の回数・
  §6 の検定数は、判定が終わるまで動かさない
- **判定の結果はこの文書に追記する。** 解釈・採否の判断は設計責任者（CLAUDE.md）
- **判定式・閾値・並び順・配信は、この判定の結果では自動的に変わらない**
